"""
Общие константы приложения NeoFin AI.

Этот модуль содержит все константы, используемые в различных частях приложения,
для устранения дублирования и обеспечения консистентности.
"""

# =============================================================================
# BACKWARD COMPATIBILITY ALIASES
# =============================================================================
# Aliases for old constant names to avoid breaking existing imports
DEFAULT_TIMEOUT = AI_TIMEOUT  # Deprecated: use AI_TIMEOUT
DEFAULT_RETRIES = AI_MAX_RETRIES  # Deprecated: use AI_MAX_RETRIES
RETRY_DELAY = AI_RETRY_DELAY  # Deprecated: use AI_RETRY_DELAY
RETRY_BACKOFF = AI_RETRY_BACKOFF  # Deprecated: use AI_RETRY_BACKOFF
MAX_FILE_SIZE_MB = MAX_FILE_SIZE // (1024 * 1024)  # Derived constant

# =============================================================================
# PDF FILE VALIDATION
# =============================================================================

# PDF magic header для валидации файлов
PDF_MAGIC_HEADER: bytes = b"%PDF-"

# Максимальный размер файла (50 MB)
MAX_FILE_SIZE: int = 50 * 1024 * 1024  # 50 MB

# Размер заголовка для проверки magic numbers
MAGIC_HEADER_SIZE: int = 8  # bytes

# =============================================================================
# PDF PROCESSING
# =============================================================================

# Максимальное количество страниц для обработки (защита от DoS)
MAX_PDF_PAGES: int = 100

# Размер чанка для чтения файлов (8 KB)
FILE_CHUNK_SIZE: int = 8 * 1024  # 8 KB

# =============================================================================
# AI SERVICE
# =============================================================================

# Timeout для AI запросов (секунды)
AI_TIMEOUT: int = 120

# Максимальное количество попыток AI запроса
AI_MAX_RETRIES: int = 3

# Базовая задержка между попытками (секунды)
AI_RETRY_DELAY: float = 1.0

# Множитель экспоненциальной задержки
AI_RETRY_BACKOFF: float = 2.0

# =============================================================================
# DATABASE
# =============================================================================

# Timeout для NLP анализа (секунды)
NLP_TIMEOUT: float = 60.0

# Максимальный размер текста для хранения в БД
MAX_TEXT_LENGTH: int = 5000

# Максимальное количество таблиц для хранения в БД
MAX_TABLES_COUNT: int = 10

# =============================================================================
# SCORING
# =============================================================================

# Веса коэффициентов для интегрального скоринга
SCORING_WEIGHTS = {
    "Коэффициент текущей ликвидности": 0.3,
    "Коэффициент автономии": 0.2,
    "Рентабельность активов (ROA)": 0.2,
    "Рентабельность собственного капитала (ROE)": 0.2,
    "Долговая нагрузка": 0.1,
}

# Пороги для нормализации коэффициентов
SCORING_TARGETS = {
    "Коэффициент текущей ликвидности": 2.0,
    "Коэффициент автономии": 0.5,
    "Рентабельность активов (ROA)": 0.1,
    "Рентабельность собственного капитала (ROE)": 0.2,
    "Долговая нагрузка": 2.0,  # max_acceptable
}

# Пороги для определения уровня риска
RISK_THRESHOLDS = {
    "low": 80,    # score >= 80
    "medium": 60, # score >= 60
    "high": 0,    # score < 60
}

# =============================================================================
# RATE LIMITING
# =============================================================================

# Стандартный лимит запросов
DEFAULT_RATE_LIMIT: str = "100/minute"

# Лимит для upload endpoints (более строгий)
UPLOAD_RATE_LIMIT: str = "10/minute"

# Лимит для health/check endpoints (менее строгий)
HEALTH_RATE_LIMIT: str = "1000/minute"

# =============================================================================
# CORS
# =============================================================================

# CORS origins по умолчанию (localhost для разработки)
DEFAULT_CORS_ORIGINS: list[str] = [
    "http://localhost",
    "http://localhost:80",
    "http://127.0.0.1",
    "http://127.0.0.1:80",
    "http://localhost:3000",
    "http://127.0.0.1:3000",
]

# CORS methods по умолчанию
DEFAULT_CORS_METHODS: list[str] = ["GET", "POST", "PUT", "DELETE", "OPTIONS"]

# CORS headers по умолчанию
DEFAULT_CORS_HEADERS: list[str] = [
    "Content-Type",
    "Authorization",
    "X-Requested-With",
    "X-API-Key",
]

# =============================================================================
# LOGGING
# =============================================================================

# Логирование по умолчанию
DEFAULT_LOG_LEVEL: str = "INFO"
DEFAULT_LOG_FORMAT: str = "text"  # или "json"

# =============================================================================
# HEALTH CHECK
# =============================================================================

# Интервал health check (секунды)
HEALTHCHECK_INTERVAL: int = 30

# Timeout health check (секунды)
HEALTHCHECK_TIMEOUT: int = 10

# Количество попыток health check
HEALTHCHECK_RETRIES: int = 3
