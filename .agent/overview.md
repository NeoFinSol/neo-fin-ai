# NeoFin AI — Обзор проекта

## Статус
- **Текущая агентская сессия**: 2026-04-20 — создан recovery-контур для безопасной интеграции веток вокруг `Math Layer v2 Wave 4.5`. Сохранены страховочные ветки `codex/preserve-main-dirty-2026-04-20` и `codex/preserve-math-layer-v1-dirty-2026-04-20`, рабочее дерево переведено на `codex/wave45-recovery-2026-04-20` от `origin/main`. После re-check выяснено: `feat/math-wave3-layer-v2` уже совпадает по дереву с `origin/main`, `claude/festive-nash` содержит в основном устаревший/шумный surface. В recovery перенесены только два полезных non-duplicative тестовых инварианта: vocabulary lock для `SuppressionPolicy` и consumer-contract test, что `calculate_ratios()` читает весь legacy export из `project_legacy_ratios()`. Targeted verification зелёная: `tests/test_api.py tests/test_math_contracts.py tests/test_ratios.py` → `13 passed`.
- **2026-04-20 — Math Layer v2 Wave 4 (outward reason governance) — ЗАКРЫТА в коде на ветке `feat/math-wave3-layer-v2`:** единый реестр `reason_codes.py`, финальная резолюция `reason_resolution.py`, emission guard на `DerivedMetric`, trace-слой `trace_reason_semantics.py`, удалён shim `resolver_reason_codes.py`, governance-тесты включены в репозиторий через `.gitignore` (исключения для `tests/test_reason_*.py`, `test_comparative_reason_governance.py`, `test_wave4_engine_reason_governance.py`, `test_ratio_helper_safety.py`). Зафиксированный долг: TD-023–TD-026 в `.agent/tech_debt_backlog.md`; нормативный `math_layer_v2_wave4_spec.md` — вне репо до отдельной публикации. Трекнутая заметка: `docs/MATH_LAYER_V2_WAVE4_CLOSURE.md`.

- **Текущая агентская сессия**: 2026-04-19 — Math Layer v2 Wave 2 COMPLETE (Tasks A-F: registry validation, proof metric, denominator classification, policy evaluation, engine gate, ratio helper hardening). Подробности ниже.

- **Math Layer v2 — Wave 2: ЗАВЕРШЕНА** (2026-04-19)
  - Реализованы все 6 задач (Task A-F) по denominator policy hardening
  - Task A: Registry validation — machine-checkable ratio-like identity via `is_ratio_like()`, explicit denominator declaration enforced
  - Task B: Proof metric declaration — `_wave2_proof_allow_any_non_zero` with ALLOW_ANY_NON_ZERO policy, non-export boundary verified
  - Task C: Canonical denominator classification — `classify_denominator()` in validators.py with centralized threshold (DENOMINATOR_EPSILON = 1e-9)
  - Task D: Canonical denominator policy evaluation — `evaluate_denominator_policy()` with full matrix coverage (STRICT_POSITIVE + ALLOW_ANY_NON_ZERO × all classes)
  - Task E: Engine denominator gate — `_validate_denominator_policy()` integrates classifier + evaluator, deterministic refusal mapping (Section 13)
  - Task F: Canonical ratio helper fail-safe hardening — `_ratio()` guards: missing inputs (F2), non-finite (F3), zero/signed-zero (F4), near-zero (F5), no raw divide before guards pass (F6), no-crash guarantee (F10-F11)
  - Тесты: 120 passed (denominator classification, policy evaluation, engine gate, ratio helper safety, proof metric registration, registry validation)
  - Архитектурные инварианты: dual-layer safety (engine gate + local helper guard), structured refusal only, determinism, non-breaking
  - SOLID/Clean Code verification: функции ≤ 20 строк, complexity ≤ 5, вложенность ≤ 3 уровня, guard clauses pattern
  - **Следующий шаг**: Commit Wave 2 implementation → PR

- **AI Agent Control Center Demo: ЗАВЕРШЕНА** (2026-04-19)

- **AI Agent Control Center Demo: ЗАВЕРШЕНА** (2026-04-19)
  - Создана демонстрационная страница интеграции AgentBrowser + MCP + Supabase
  - Файлы: `src/pages/AIAgentControlCenter.tsx` (569 строк), `src/components/AIAgentControlCenter/index.tsx` (370 строк), `types.ts` (108 строк), `styles/AIAgentControlCenter.css` (327 строк)
  - Ключевые фичи: управление агентами, система задач с приоритетами, MCP инструменты, AgentBrowser iframe, real-time updates simulation, statistics dashboard
  - Технологии: React 19, TypeScript, Ant Design v5.29.3, @supabase/supabase-js v2.89.0, MCP integration
  - Исправлены проблемы: npm registry switched to official, установлен @supabase/supabase-js
  - Доступно по адресу: http://localhost:8000/ai-agent-control-center

- **Math Layer v2 — Wave 1a: ЗАВЕРШЕНА** (2026-04-19)
  - Реализованы все 30 задач (W1A-001–W1A-030)
  - Новые модули: `normalization.py`, `rounding.py`, `finalization.py`, `numeric_errors.py`, `projections.py` (hardened), `engine.py` (wired), `comparative.py` (hardened)
  - Архитектурные инварианты: Decimal→float только через projections.py, нет NaN/Inf/-0.0 в output, engine не определяет своих helpers
  - SOLID/Clean Code verification: flake8 PASS, complexity 4 функции 6-7 (guard clauses acceptable)
  - **Следующий шаг**: pytest verification → commit → PR

- **Security items (TD-001, TD-011, TD-012): ЗАКРЫТЫ** (2026-04-19)
  - TD-001: `/metrics` теперь требует `Depends(get_api_key)`. Добавлены 3 теста на auth (401 без ключа, 200 с валидным, 401 с невалидным).
  - TD-011: добавлено `!tests/test_wave8_websocket_auth.py` в `.gitignore` — файл больше не игнорируется.
  - TD-012: `# noqa: F401` заменён на `__all__` в `circuit_breaker.py` и `core/security.py` — CodeQL false positive устранён.
  - Verification: 40 passed

- **Security items (TD-001, TD-011, TD-012): ЗАКРЫТЫ** (2026-04-19)
  - TD-001: `/metrics` теперь требует `Depends(get_api_key)`. Добавлены 3 теста на auth (401 без ключа, 200 с валидным, 401 с невалидным).
  - TD-011: добавлено `!tests/test_wave8_websocket_auth.py` в `.gitignore` — файл больше не игнорируется.
  - TD-012: `# noqa: F401` заменён на `__all__` в `circuit_breaker.py` и `core/security.py` — CodeQL false positive устранён.
  - Verification: 40 passed

- **Math Layer v2 — Wave 1b: ЗАВЕРШЕНА И ПРИНЯТА** (2026-04-19)
  - Итерация 1 (Блоки 1A+1B): model contract migration + builder migration. `DerivedMetric` получил `canonical_value: Decimal | None`, `projected_value: float | None`, `value` → `@computed_field`. Lifecycle validator блокирует F1/F2. Engine мигрирован на три-поля.
  - Итерация 2: serializer + surface policy + lifecycle enforcement. Exposure policy задокументирована. JSON numeric types. Lifecycle matrix. No mixed-authority paths.
  - Итерация 3: полный test package (B1-020–028). 104 новых теста.
  - Итерация 4: benchmark (0.97x overhead — PASS) + closure (B1-029–034). Все 4 review passes пройдены.
  - Итого тестов: 429 passed
  - Benchmark: `.agent/math_layer_v2_wave1b_benchmark.md`
  - **Следующий шаг**: Wave 2 — denominator policy hardening (TD-003)

- **Math Layer v2 — Wave 1a: ЗАВЕРШЕНА И ПРИНЯТА** (2026-04-18)
  - Реализованы все 30 задач (W1A-001–W1A-030)
  - 325 тестов: unit + PBT + integration + structural + anti-fake-fix + compatibility snapshots
  - Аудит пройден: 7 findings, 4 закрыты, 3 → tech debt (TD-013–015)
  - Инварианты: `DerivedMetric.value: float | None` не изменён, REST/WS shape не изменён, Decimal→float только через `projections.py`, нет NaN/Inf/-0.0 в публичном output
  - Новые модули: `normalization.py`, `rounding.py`, `finalization.py`, `numeric_errors.py` (core), `projections.py` (hardened), `engine.py` (wired), `comparative.py` (hardened)
  - Новые тест-файлы: `test_math_normalization.py`, `test_math_rounding.py`, `test_math_finalization.py`, `test_math_projections.py`, `test_math_engine_integration.py`, `test_math_comparative_hardening.py`, `test_math_wave1a_structural.py`, `test_math_wave1a_batch5.py`
  - **Следующий шаг**: Wave 1b — public `canonical_value`/`projected_value`/`value=@computed_field`, Decimal performance benchmark, JSON numeric type contract. Спека: `.agent/math_layer_v2_wave1b_spec.md`

- **Тех долг (открытые items):** TD-001–TD-015. Самые срочные: TD-001 (`/metrics` без auth), TD-002 (CSP), TD-003 (denominator policy gap — Wave 2).

- **Текущая агентская сессия**: 2026-04-16 — завершены `audit-findings-remediation` (F2–F9), `Wave 6 — Layering Cleanup` (ARCH-001, ARCH-002), `Wave 8 — WebSocket Auth` (SEC-002). Verification: `183 passed`.

- **Wave 8 — WebSocket Auth** (SEC-002): добавлена аутентификация на `/ws/{task_id}` через query param `?api_key=...`. Новый pure helper `_is_ws_auth_valid()` с guard clauses и `hmac.compare_digest`. Отклонение до `ws_manager.connect()`. Именованная константа `_WS_CLOSE_UNAUTHORIZED = 4001`. Логирование без утечки ключа. `import logging` заменён на `get_logger`. Добавлен `tests/test_wave8_websocket_auth.py` с 25 тестами (unit + 4 PBT + integration). SOLID/Clean Code verification pass пройден.

- **audit-findings-remediation** (F2–F9): (1) F2 — `_resolve_database_url()` теперь бросает `RuntimeError` при `CI=1` без URL вместо возврата `None`; (2) F3 — убран stale `self.model` из `OllamaAgent.__init__`; (3) F4 — singleton `ollama_agent` создаётся с `timeout=app_settings.ai_timeout`; (4) F5 — добавлены validators для `ai_timeout [1,600]`, `ai_retry_count [0,10]`, `ai_retry_backoff [0.1,60.0]` в `AppSettings`; (5) F6 — удалён мёртвый `except CircuitBreakerOpenError` из `AIService.invoke()`; (6) F7 — удалён мёртвый метод `_check_state_transition()` из `CircuitBreaker`; (7) F8 — `ollama_agent.py` переведён на проектный `get_logger`; (8) F9 — `CircuitBreakerOpenError` теперь принимает `details`. Добавлены `tests/test_ollama_agent.py`, `tests/test_ai_settings_validators.py`, `tests/test_db_database_url.py`, `tests/test_audit_remediation_commit4.py` с PBT-сюитами (Hypothesis). Verification: `183 passed`.

- **Wave 6 — Layering Cleanup** (ARCH-001, ARCH-002): (1) ARCH-001 — `SELECT 1` убран из `src/routers/system.py`; новая функция `check_database_connectivity() -> bool` добавлена в `src/db/crud.py`; роутер больше не импортирует `sqlalchemy.text` и `get_engine`; (2) ARCH-002 — `get_engine()` больше не читает `app_settings` напрямую; добавлен frozen dataclass `DatabaseConfig` с `from_settings()`; `get_engine(config: DatabaseConfig | None = None)` принимает конфигурацию явно. Обновлены `tests/test_routers_system.py`, `tests/test_routers_system_full.py`. Добавлен `tests/test_wave6_layering_cleanup.py` с PBT. Verification: `74 passed`.

- **Дополнительно**: убраны unused imports (`asyncio` в `base_agent.py`, `Any` в `base_agent.py`, `pytest` в `test_core_security.py`, `os` в `settings.py`); исправлены длинные строки в `database.py`, `exceptions/__init__.py`, `circuit_breaker.py`, `settings.py`.

- **Текущая агентская сессия**: 2026-04-15 — завершён `Wave 5A/5B — Runtime / Settings / Metadata Reliability` прямым коммитом в `main` (`628a7fb`). W5-1 (SETTINGS-001): убран бесполезный `AppSettings(_case_sensitive=False)` fallback — validators уже обрабатывают все невалидные значения. W5-2 (CONTRACT-001): подтверждено, что `extraction_metadata` не трогается `mask_analysis_data`; добавлены 4 regression теста. W5-3 (HC-002): Ollama теперь использует shared `aiohttp.ClientSession` через `_get_ollama_session()`, закрывается в `AIService.close()`. W5-4 (DOC-006): defer. Verification: `56 passed`.
- **Текущая агентская сессия**: 2026-04-15 — завершён `Wave 4A/4B — AI Contract Repair` на ветке `codex/wave4-ai-contract-2026-04-15`. Закрыты ARCH-003, ARCH-004, ARCH-005, ARCH-007, TEST-003: (1) дубликат `ConfigurationError` в `agent.py` удалён — теперь импортируется из `base_agent`; (2) `GigaChatAgent` и `HuggingFaceAgent` переведены с `ValueError` на `ConfigurationError`; (3) добавлено публичное свойство `is_configured` в `BaseAIAgent`; (4) `ai_service._configure()` использует `is_configured` вместо `._configured`; (5) добавлен `tests/test_wave4_ai_contract.py` с 27 тестами на единую иерархию ошибок и публичный контракт. Финальная verification closure: `111 passed`.
- **Текущая агентская сессия**: 2026-04-15 — завершён `SOLID & Clean Code remediation pack` на новой ветке `codex/solid-clean-code-2026-04-15`. Закрыты все 10 пунктов плана: Wave 3A/3B (BUG-003 + TEST-004) — upload validation parity для multi-analysis через новый `src/utils/upload_validation.py`; P1.1 (DIP) — роутеры больше не импортируют друг из друга; P1.2 (LSP) — `AnalysisAlreadyExistsError` переведён на domain exception без наследования `IntegrityError`; P2.1 (Clean Code) — именованные константы для score caps; P2.2 (Clean Code) — `FILE_CHUNK_SIZE`/`MAGIC_HEADER_SIZE` вместо литералов; P2.3 (Clean Code) — удалён неиспользуемый `from pathlib import Path`; P3 (OCP) — hardcoded `"retail_demo"` заменён на data-driven `_PROFILE_PEER_CONTEXT`/`_PROFILE_LEVERAGE_BASIS`; P4 (DIP) — `app_settings` убран из `scoring.py`, profile передаётся явно через `calculate_score_with_context(profile=...)`; P5.1/P5.2/P5.3 (SRP) — декомпозированы `_run_extraction_phase` (~80→25 строк), `_try_llm_extraction` (~90→20 строк), `_apply_scoring_methodology_adjustments` (~55→20 строк). Финальная verification closure зелёная: `213 passed`. Hygiene: `black --check`, `isort --check-only` чистые.
- **Текущая агентская сессия**: 2026-04-14 — `Wave 2A / BUG-002` закрыт как narrow semantic remediation на ветке `codex/immediate-hardening-wave-2026-04-14`. После repo-wide recheck live extractor routing очищен от `2300 -> net_profit`: из `src/analysis/extractor/rules.py` удалён table/code map `2300`, mirrored `_TEXT_LINE_CODE_MAP` в `src/analysis/extractor/legacy_helpers.py` синхронно переведён на `2400`-only semantics, а post-fix search по `src/analysis/extractor/*.py` больше не находит `2300` в live Python routing. Добавлен targeted regression pack без fixture mass-refresh: table precedence (`2300 before 2400`), fail-closed `only-2300`, explicit `2400` non-regression, debug-trace checks на отсутствие `2300` в `net_profit` candidate set и downstream scoring characterization, который теперь пинит canonical `ROS=0.01` вместо buggy `0.09`. Closure verification зелёная: `python -m pytest tests/test_pdf_extractor.py tests/test_extractor_guardrail_debug.py tests/test_scoring.py -q` → `93 passed`; `git diff --check` clean по syntax/hunks. Явный local `code_review` pass выполнен: блокирующих замечаний не найдено, public contracts/metric set не менялись, fixture updates не потребовались.
- **Текущая агентская сессия**: 2026-04-14 — добавлен отдельный handoff-файл `.agent/math_layer_v2_target.md` с target-state для `Math Layer v2`. Это не implementation plan, а инженерный ориентир: зачем v2 нужна поверх `Math Layer v1/v1.5`, какие invariants нельзя ломать, какие workstreams должны войти в v2 (typed boundaries, comparative source-of-truth, resolver architecture, metric coverage expansion, downstream simplification) и в каком порядке их лучше делать. Если в новом диалоге нужно обсуждать развитие математики, начинать стоит с этого файла, а уже потом писать отдельный plan/spec.
- **Текущая агентская сессия**: 2026-04-14 — для безопасного перехода в новый диалог добавлены два handoff-файла: `.agent/audit_wave_execution_board.md` и `.agent/audit_findings_registry.md`. Первый фиксирует весь verification-first wave board, текущий прогресс и рекомендуемый следующий шаг (`Wave 2A — BUG-002`). Второй хранит per-finding registry: что уже закрыто в immediate wave и post-push follow-ups, а что остаётся в pending verification backlog. Если продолжать audit в новом чате, начинать нужно с этих двух файлов, затем при необходимости открывать `superpowers/audit/2026-04-14-ultra-deep-audit-final-synthesis.md`.
- **Текущая агентская сессия**: 2026-04-14 — закрыт дополнительный post-push lint follow-up по той же ветке `codex/immediate-hardening-wave-2026-04-14`. Remote Code Linting падал на `isort --profile black --check-only` из-за import order в `tests/test_core_ai_service.py`; import block синхронизирован с canonical `isort` ordering (`_TIMEOUT_RETRY_EXHAUSTED, AIService`). Локальная verification closure зелёная: `python -m isort --profile black --check-only src tests` проходит без ошибок, а `python -m pytest tests/test_core_ai_service.py -q` → `20 passed`.
- **Текущая агентская сессия**: 2026-04-14 — закрыт post-push feedback pack по immediate hardening branch `codex/immediate-hardening-wave-2026-04-14`. Исправлены три реальных хвоста, поднятых CI/review: (1) `.github/workflows/code-quality.yml` больше не type-check’ит удалённый orphan path `src/models/database/user.py` и теперь держит canonical ORM target `src/db/models.py`; (2) `src/core/auth.py::_api_keys_match()` переведён на `hmac.compare_digest()` по UTF-8 bytes, поэтому non-ASCII header/API key больше не валят auth path `TypeError` и деградируют в обычный mismatch (`401` / `None`); (3) `src/core/ai_service.py` получил explicit timeout-exhaustion sentinel в retry path, так что exhausted timeout retries теперь записываются как failure в circuit breaker и `metrics`, а не как ложный success. Дополнительно `src/routers/system.py::health_check()` сжат helper-ами без смены semantics, чтобы не оставлять function-length хвост на changed surface. Verification closure зелёная: `python -m pytest tests/test_core_auth.py tests/test_core_ai_service.py tests/test_routers_system.py tests/test_routers_system_full.py tests/test_github_workflows.py -q` → `75 passed`; локальный mypy slice под CI command с `src/db/models.py` тоже зелёный.
- **Текущая агентская сессия**: 2026-04-14 — выполнен `Orphan Models Decision` pack по `BUG-001` в delete-path варианте после execution-time repo-wide recheck. `src/models/database/user.py` и `project.py` удалены как unsupported dead boundary; директория `src/models/database/` убрана после проверки, что кроме `__pycache__` там ничего не осталось. `src/db/models.py` остаётся canonical supported ORM surface. Добавлен machine-checkable dead-path guard в `tests/test_dead_paths.py`: canonical ORM import smoke через `src.db.models`, проверка отсутствия orphan files и scan по maintained executable surface (`src/tests/scripts/migrations + top-level tooling`) на остаточные ссылки `src.models.database`. Verification closure зелёная: `python -m pytest tests/test_dead_paths.py tests/test_migrations.py -q` → `6 passed`.
- **Текущая агентская сессия**: 2026-04-14 — выполнен `Util/Config Hardening` pack по `ARCH-008`, `BUG-004`, `SEC-004` без расширения AI/runtime scope. В `src/utils/retry_utils.py` `retry_with_timeout()` теперь retry’ит только `asyncio.TimeoutError`, поэтому non-timeout exceptions больше не маскируются broad retry и fail fast на caller boundary; в `src/utils/masking.py` введён `MAX_FRACTIONAL_MASK_WIDTH = 4`, который режет только fractional mask segment после текущего fractional-length calculation и не меняет integer/sign/zero semantics; в `alembic.ini` credential-bearing URL заменён на intentional nonsecret placeholder `postgresql+psycopg2://user:pass@localhost/dbname` с явным комментарием про `DATABASE_URL` через `env.py`. Добавлены/обновлены `tests/test_retry_utils.py`, `tests/test_masking.py`, `tests/test_migrations.py`, `tests/test_core_ai_service.py`. Targeted verification closure зелёная: `python -m pytest tests/test_retry_utils.py tests/test_masking.py tests/test_migrations.py tests/test_core_ai_service.py -q` → `40 passed`.
- **Текущая агентская сессия**: 2026-04-14 — выполнен `Auth/System Hardening` pack по `SEC-001`, `SEC-003`, `HC-003` без расширения security scope. В `src/core/auth.py` добавлен private helper `_api_keys_match()` на `hmac.compare_digest`; exact-match semantics и guard ordering сохранены, `optional_auth()` по-прежнему остаётся non-raising on mismatch. В `src/routers/system.py` введён shared helper `_current_utc_timestamp()` на `datetime.now(timezone.utc)` и переведены `/system/health` и `/system/healthz` на UTC-aware ISO timestamps; `/system/ready` больше не возвращает `str(e)` и теперь отдаёт фиксированное `503 detail="Service not ready: database connection failed"` при сохранении server-side exception logging. Синхронизированы `tests/test_core_auth.py`, `tests/test_routers_system.py`, `tests/test_routers_system_full.py` и `docs/API.md`. Targeted verification closure зелёная: `python -m pytest tests/test_core_auth.py tests/test_routers_system.py tests/test_routers_system_full.py -q` → `33 passed`.
- **Текущая агентская сессия**: 2026-04-14 — **Ultra-Deep Audit Phase 8 Final Synthesis завершён.** Полный аудит backend (7 фаз investigation + 1 synthesis) формализован в `superpowers/audit/2026-04-14-ultra-deep-audit-final-synthesis.md`. Итого: 2 BLOCKING (orphan `src/models/database/` с несуществующим import), ~25 HIGH issues (security: timing-attack auth + unauth WS; concurrency: circuit breaker race + Ollama session leak; architecture: ConfigurationError LSP violation + _configured encapsulation breach + DIP violations + raw SQL in router; function length: 5 функций >80 строк; math: reason code drift + untyped boundaries + denominator policy gap), 8 MEDIUM (dead code + naming + docs mismatches), 10 test gaps. Roadmap: Immediate (I1-I5: BLOCKING + security + concurrency), Short-term (S1-S8: SOLID + typing), Medium-term (M1-M10: decomposition + dead code + docs). Рекомендация: Immediate первым, параллельно с продуктовыми задачами.
- **Текущая агентская сессия**: 2026-04-13 — реализована `Confirmed Debt Remediation Wave` в worktree `codex/confirmed-debt-remediation` без расширения product scope `Math Layer v1`. Закрыты все 5 подтверждённых проблемы из `main/local_notes`: (1) domain constraints больше не живут в `validators.py` как hardcoded semantic list — `MetricDefinition` теперь несёт `non_negative_inputs`, а `INPUT_DOMAIN_CONSTRAINTS`/`get_input_domain_constraint()` строятся производно из canonical registry; (2) naming source of truth теперь единый — `MetricDefinition` хранит `legacy_label` и `frontend_key`, а `LEGACY_RATIO_NAME_MAP` и `RATIO_KEY_MAP` стали derived lookup maps из `src/analysis/math/registry.py`, локальная дублирующая map в `src/analysis/ratios.py` удалена; (3) reflection dispatch в `src/analysis/extractor/pipeline.py` удалён, вместо `inspect.signature(...)` введён explicit typed callable contract через `StageCollector`/`MetadataBuilder`; (4) `src/routers/pdf_tasks.py::upload_pdf()` разрезан на thin boundary helpers (`_save_uploaded_pdf`, `_resolve_requested_provider`, `_create_upload_analysis_record`, `_dispatch_upload_task`) и больше не протекает temp-file при provider validation/dispatch failure; (5) `src/tasks.py::process_pdf()` приведён к thin orchestration wrapper (`34` строк) с phase-based helpers `_run_analysis_extraction_step`, `_run_analysis_scoring_step`, `_run_analysis_ai_step`, `_finalize_analysis_success` без изменения status flow, cancellation/heartbeat semantics и финального payload shape. В touched verification contour добавлены/обновлены regression tests на registry-driven domain constraints, canonical naming projections, anti-reflection pipeline contract, `upload_pdf` cleanup и ordered phase updates в `process_pdf`.
- **Текущая агентская сессия**: 2026-04-12 — `Math Layer v1 Foundation` доведён до завершённого execution-pack в отдельном worktree `codex/math-layer-v1`. Закрыты три ключевых пакета: (1) P0 containment legacy scoring/ratios — `_normalize_inverse()` больше не превращает `value <= 0` в “идеал”, denominator safety вынесена в явную conservative semantics; (2) создан новый пакет `src/analysis/math/` как source-of-truth foundation: `contracts.py`, `policies.py`, `validators.py`, `precompute.py`, `registry.py`, `engine.py`; engine принимает только typed `MetricInputRef`, registry immutable (`MappingProxyType`), suppression/denominator policies реально участвуют в runtime, а `DerivedMetric`/trace contract уже существует; (3) `src/analysis/ratios.py` превращён в legacy projection bridge через `MathEngine -> projections.py`, локальные formula helpers удалены, а safe v1 subset ограничен `current_ratio`, `absolute_liquidity_ratio`, `ros`, `equity_ratio`, тогда как EBITDA/debt/unsupported legacy ratios уходят в `None` через `suppressed`/missing projection semantics. Добавлены новые regression suites: `tests/test_math_containment.py`, `tests/test_math_contracts.py`, `tests/test_math_engine.py`, `tests/test_math_projection_bridge.py`; также обновлены `tests/test_ratios.py` и `tests/test_scoring.py` под consumer-only semantics suppressed metrics. Финальный changed-surface verification этой волны зелёный: `python -m pytest tests/test_math_contracts.py tests/test_math_engine.py tests/test_math_projection_bridge.py tests/test_math_containment.py tests/test_ratios.py tests/test_scoring.py tests/test_tasks.py tests/test_api.py -q` → `66 passed, 5 skipped`. Commit-ы этой волны на ветке worktree: `fix(math): contain unsafe inverse and denominator handling`, `feat(math): add domain contracts and engine foundation`, `refactor(ratios): route legacy ratios through math bridge`, `test(math): add closure checks and sync agent docs`. Следующий шаг в этой ветке уже не foundation, а integration decision: либо merge-back в основной рабочий контур, либо follow-up wave для расширения safe metric scope и richer task/API surfacing.
- **Текущая агентская сессия**: 2026-04-12 — реализована `Decision Transparency Wave` (Волна 7): typed, deterministic DecisionTrace projection layer поверх extractor/scoring структур. Все 5 подволн выполнены: (1) core structures — enums, dataclasses, helpers, trace builder в `src/analysis/extractor/decision_trace.py`; (2) pipeline integration — winner_map в ExtractorStageTrace, LLM merge reasons, issuer override tracing; (3) final trace + debug endpoint — `build_decision_trace()` в `process_pdf()` при `debug_trace=True`, `GET /debug/decision-trace/{task_id}`; (4) validation — 61 тест в `test_decision_trace.py`, 4 в `test_debug_endpoint.py`; (5) wire contract — DecisionTrace types в `interfaces.ts`, Pydantic schemas в `schemas.py`. 8 коммитов: `feat(trace): add DecisionTrace enums and ReasonCode alias`, `feat(trace): add DecisionTrace dataclasses with TYPE_CHECKING imports`, `feat(trace): add candidate_id, guardrail mapper, serialization helpers`, `feat(trace): add trace builder with proper candidate classification`, `feat(trace): add winner_map to ExtractorStageTrace, populate from canonical candidate_id`, `feat(trace): integrate DecisionTrace into process_pdf pipeline with debug_trace flag`, `feat(trace): add debug decision-trace endpoint`, `feat(trace): expose DecisionTrace in wire payload and Pydantic schemas`, `test(trace): validate decision trace completeness and usefulness`.
- **Текущая агентская сессия**: 2026-04-07 — выполнена `Stabilization Wave for noisy --suite all evidence path` без runtime retune и без corpus expansion. Commit A закрыт как чистый bugfix: в `src/analysis/extractor/ocr.py` введён non-recursive OCR adapter поверх captured legacy callable, а `src/analysis/extractor/tables.py` больше не патчит fallback на façade OCR entrypoint напрямую; это устранило self-referential re-entry в table-path OCR fallback и сняло `maximum recursion depth exceeded` при `extract_tables()` на camelot-empty path. Для фикса добавлен regression в `tests/test_pdf_extractor.py`, который жёстко проверяет intended control flow `Camelot -> OCR adapter`, один OCR pass и отсутствие recursion при сохранении façade-bound monkeypatch contract (`camelot`, `convert_from_path`, `pytesseract`, `MAX_OCR_PAGES`).
- **Текущая агентская сессия**: 2026-04-07 — Commit B этой же волны довёл calibration CLI до quiet-by-default behavior. `src/analysis/extractor/calibration.py` теперь захватывает fixture-backed Camelot/OCR warnings и extractor warning logs как internal diagnostics events с append-only multiplicity и capture order, переносит их в internal evidence schema (`aggregate/suite/per-case diagnostics`, `unexpected_diagnostics`) и не даёт им утекать в raw stdout/stderr во время `run_extractor_confidence_calibration.py`. Дополнительно сам CLI `scripts/run_extractor_confidence_calibration.py` точечно suppress’ит ARC4 deprecation noise на входе, по аналогии с real-PDF tests, чтобы `--suite all` с committed fixtures отрабатывал без сырого console spam. Public app/frontend contracts не менялись; diagnostics остаются strictly internal calibration evidence only.
- **Текущая верификация**: mandatory closure зелёная — `python -m pytest tests/test_pdf_extractor.py tests/test_pdf_extractor_facade.py tests/test_analysis_pdf_extractor.py tests/test_extractor_confidence_calibration.py tests/test_pdf_real_fixtures.py -q` → `142 passed`; optional local characterization тоже зелёный — `RUN_LOCAL_PDF_REGRESSION=1 python -m pytest tests/test_pdf_local_magnit_regression.py -q` → `6 passed`. Реальный `python scripts/run_extractor_confidence_calibration.py --suite all --format json --output %TEMP%\\extractor_calibration_evidence_stabilized.json` завершился `0` и больше не печатает raw Camelot/OCR/ARC4 noise в консоль; diagnostics при этом действительно попали в evidence (`camelot_warning=12` для baseline и candidate на текущем committed corpus, `diagnostic cases=3`, `unexpected=0`). Hygiene gate закрыт: `black --check`, `isort --check-only`, `git diff --check` по touched files clean. Явный local `code_review` pass выполнен: блокирующих замечаний по волне не найдено.
- **Текущая агентская сессия**: 2026-04-06 — реализована `Russian Real-Fixture Anchor Expansion Wave` как prepare-only продолжение corpus/harness hardening без нового runtime retune. В committed real-fixture corpus добавлены три русских Magnit PDF (`magnit_2025_q1_scanned`, `magnit_2023_ifrs`, `magnit_2025_h1_ifrs`) под `tests/data/pdf_real_fixtures/` и `manifest.json` с `kind: calibration_anchor`, manifest-backed `sha256` identity и selective expected subsets; default smoke loader в `tests/test_pdf_real_fixtures.py` теперь жёстко берёт только `kind == "smoke"`, а отдельные invariants проверяют, что `calibration_anchor` разрешён для calibration harness и исключён из smoke path. `src/analysis/extractor/calibration.py` расширен новым `pipeline_mode="force_ocr"` как OCR-only execution mode (`extract_text_from_scanned` + `tables=[]`), cross-platform manifest-only fixture path resolution и узким merge evidence contract для `authoritative_override` (`winner/source/authoritative_override/reason_code` без общего explainability creep). `tests/data/extractor_confidence_calibration/gated.json` теперь содержит Russian real-fixture anchors для OCR boundary, annual IFRS threshold boundary, debt-component table winners и issuer override protection; `docs/EXTRACTOR_CONFIDENCE_CALIBRATION_CORPUS.md`, `tests/data/pdf_real_fixtures/README.md` и checked-in evidence pack синхронизированы под новый corpus. Свежая verification closure зелёная: `python -m pytest tests/test_extractor_confidence_calibration.py tests/test_pdf_real_fixtures.py tests/test_pdf_extractor.py -q` → `83 passed`; `RUN_LOCAL_PDF_REGRESSION=1 python -m pytest tests/test_pdf_local_magnit_regression.py -q` → `6 passed`; `black --check`, `isort --check-only`, `git diff --check` → clean. Final checked-in evidence on `fast + gated`: `operational_accuracy 0.656 -> 0.625`, `false_accepts 2 -> 0`, `false_rejects 1 -> 8`, `survivors 23 -> 14`; это intentionally harsher corpus signal exposing new gated blind spots, а не runtime retune failure. Residual note: `run_extractor_confidence_calibration.py --suite all` может печатать legacy Camelot/OCR fallback warnings на real fixtures и давать жёсткий noisy evidence path, но extractor regression/local Magnit suite при этом остаются зелёными.
- **Текущая агентская сессия**: 2026-04-06 — реализована `Corpus Expansion + Calibration Hardening Wave` как prepare-only продолжение extractor confidence calibration без нового runtime retune. `src/analysis/extractor/calibration.py` переведён на suite-aware corpus contract: вместо одного frozen manifest теперь используются suites `tests/data/extractor_confidence_calibration/fast.json` и `tests/data/extractor_confidence_calibration/gated.json`, появились `CalibrationSuite`, `fixture_ref` resolution через минимальный contract (`id`, `filename`, `sha256`), tri-state source strictness (`unspecified/advisory/critical`), multi-metric parse cases с deterministic outcome ids `case_id::metric_key`, machine-checkable coverage/anchor audit и per-suite summaries/diffs в report export. CLI `scripts/run_extractor_confidence_calibration.py` теперь поддерживает `--suite fast|gated|all`, а docs синхронизированы через `docs/EXTRACTOR_CONFIDENCE_CALIBRATION.md` и новый `docs/EXTRACTOR_CONFIDENCE_CALIBRATION_CORPUS.md`; checked-in evidence pack перегенерирован под suite-aware corpus. Regression/verification closure для hardening wave зелёный: `python -m pytest tests/test_extractor_confidence_calibration.py tests/test_confidence_score.py tests/test_confidence_properties.py tests/test_extractor_ranking_v2.py tests/test_extractor_semantics.py tests/test_pdf_real_fixtures.py tests/test_pdf_regression_corpus.py tests/test_tasks.py -q` → `94 passed`; `python -m pytest tests/test_pdf_extractor_facade.py tests/test_analysis_pdf_extractor.py tests/test_pdf_extractor.py tests/test_api.py -q` → `122 passed`; suite-aware CLI `--suite fast`, `--suite gated`, `--suite all` отрабатывает; `black --check`, `isort --check-only` и `git diff --check` по touched calibration files чистые. Новый evidence baseline/candidate на расширенном corpus: `operational_accuracy 0.750 -> 0.875`, `false_accepts 2 -> 0`, `false_rejects 0 -> 2`, `survivors 10 -> 6`; это intentionally corpus/harness hardening signal, а не новый runtime retune.
- **Текущая агентская сессия**: 2026-04-05 — confidence calibration wave (`Internal-First+`) для extractor-а доведена до landed runtime policy без изменения public API/frontend contract. В `src/analysis/extractor/confidence_policy.py` появился declarative-first runtime policy layer с baseline/candidate profiles, quality bands, structural bonus, conflict/guardrail penalties и `strong_direct_threshold`; `src/analysis/extractor/semantics.py` и `src/analysis/extractor/ranking.py` переведены на него как на single source of truth для confidence math. Добавлен permanent harness `src/analysis/extractor/calibration.py` + CLI `scripts/run_extractor_confidence_calibration.py` + frozen manifest `tests/data/extractor_confidence_calibration.json`; harness считает operational metrics по explicit decision surface, threshold sweep, trust-order invariant checks, policy diffs, shadow consumer diffs и machine-readable report. Runtime threshold reads централизованы через `app_settings.confidence_threshold` в `src/tasks.py` и `src/analysis/extractor/legacy_helpers.py`, а LLM merge path остаётся на production logic, но получил reusable helper layer в `src/analysis/extractor/runtime_decisions.py` для offline evaluation без shadow leakage в runtime. Checked-in artifacts: `docs/EXTRACTOR_CONFIDENCE_CALIBRATION.md`, `docs/EXTRACTOR_CONFIDENCE_CALIBRATION_EVIDENCE.md`, `docs/EXTRACTOR_CONFIDENCE_CALIBRATION_EVIDENCE.json`. Финальный calibrated policy intentionally changes extractor-side outcomes при том же threshold `0.5`: на frozen corpus `operational_accuracy 0.667 -> 1.000`, `false_accepts 2 -> 0`, `false_rejects 0 -> 0`, `survivors 5 -> 3`; threshold sweep показывает лучший decision quality именно на `0.50`, trust-order invariants сохранены, `issuer_fallback`/`strong_direct_threshold` не дрейфуют. Regression closure зелёный: `python -m pytest tests/test_extractor_confidence_calibration.py tests/test_confidence_score.py tests/test_confidence_properties.py tests/test_extractor_ranking_v2.py tests/test_extractor_semantics.py tests/test_extractor_evidence_emission.py tests/test_pdf_extractor_facade.py tests/test_analysis_pdf_extractor.py tests/test_pdf_extractor.py tests/test_pdf_regression_corpus.py tests/test_pdf_real_fixtures.py tests/test_tasks.py tests/test_api.py tests/test_llm_extractor.py tests/test_llm_extractor_properties.py tests/test_issuer_fallback.py -q` → `287 passed`; `black --check`, `isort --check-only` и `git diff --check` по touched files → clean. Residual note: frozen calibration corpus пока маленький, поэтому `ECE` ухудшился (`0.317 -> 0.414`) несмотря на рост operational trust; следующий quality step — расширение labeled corpus и повторная reliability calibration.
- **Текущая агентская сессия**: 2026-04-04 — выполнен следующий extractor internal-semantic pass: guardrail reason catalog и debug tooling вокруг `SemanticsDecisionLog` доведены до законченного internal-only состояния без изменения public API/frontend contract. В `src/analysis/extractor/semantics.py` добавлены minimal reason registry (`REASON_REGISTRY`), новые canonical reason constants для P&L sanity drops/replacements и current-assets guardrail replacement, типы `GuardrailEvent` и `ExtractionDebugTrace`, расширенный `SemanticsDecisionLog` (теперь хранит `metric_key`, `reason_code`, `signal_flags`, `candidate_quality`) и formatter `format_metric_decision_trace()`. В `src/analysis/extractor/guardrails.py` mutating branches больше не semantically silent: `_apply_form_like_pnl_sanity()` теперь фиксирует `DROPPED/REPLACED` events и одновременно закрывает latent parity-gap с legacy path, где при наличии обоих code candidates должны заменяться и `revenue`, и `net_profit`; `derive_missing_metrics()` пишет trace на reject/rederive ветке `current_assets`; `apply_result_guardrails()` пишет chronological `INVALIDATED` events, сохраняя public `reason_code`/`postprocess_state` поведение без изменения wire contract. В `src/analysis/extractor/ranking.py` появился helper `build_metadata_with_decision_log()`, а в `src/analysis/extractor/pipeline.py` — internal debug entrypoint `parse_financial_statements_debug()` и formatter `format_metric_debug_trace()`. Тесты расширены semantics-shaped coverage: новый `tests/test_extractor_guardrail_debug.py` проверяет P&L drop trace, current-assets drop+replacement trace, result-guardrail invalidation events, registry exhaustiveness и parity normal-vs-debug parse path; extractor-local reason usages в ключевых tests переведены на constants из `semantics.py`, чтобы избежать ad-hoc drift. Свежая верификация зелёная: `python -m pytest tests/test_extractor_guardrail_debug.py tests/test_extractor_semantics.py tests/test_extractor_ranking_v2.py tests/test_extractor_evidence_emission.py tests/test_issuer_fallback.py tests/test_llm_extractor.py tests/test_llm_extractor_properties.py tests/test_tasks.py tests/test_api.py -q` → `132 passed`; `python -m pytest tests/test_pdf_extractor_facade.py tests/test_analysis_pdf_extractor.py tests/test_pdf_extractor.py tests/test_pdf_regression_corpus.py -q` → `126 passed`; `black --check` и `isort --check-only` по затронутым extractor/tests → clean. Residual next step уже не structural: либо расширять debug inspection helpers вокруг trace consumption, либо идти в отдельную calibration wave по confidence на реальном corpus.
- **Текущая агентская сессия**: 2026-04-04 — extractor semantics wave 1 доведён до законченного V2 evidence-пакета. В `src/analysis/extractor/semantics.py` появился единый semantic source of truth: canonical evidence registry, invariants, legacy normalization helpers, semantic filtering helpers и внутренний `SemanticsDecisionLog`. Extractor internals переведены на V2 semantics end-to-end: table/text collectors размечают provenance/match/inference, approximation rules вроде `gross profit -> ebitda` публикуются как `approximation`, `issuer_fallback` стал явным `policy_override`, а `tasks.py` больше не делает manual confidence-gates и использует semantic helper’ы (`survives_confidence_filter`, `is_authoritative_override`, `is_replaceable_by_llm`, `is_strong_direct_evidence`). Дополнительно result guardrails больше не теряют provenance при invalidation: `src/analysis/extractor/guardrails.py` теперь мягко зануляет конфликтные balance metrics через `postprocess_state=guardrail_adjusted`, controlled `reason_code` и `pp:guardrail_adjusted` flag. Mixed-mode contract выровнен сквозь backend/frontend: `src/models/schemas.py`, `frontend/src/api/interfaces.ts`, `frontend/src/utils/reliability.ts`, `frontend/src/components/ConfidenceBadge.tsx`, `frontend/src/components/report/DetailedMetricsCard.tsx`, `src/analysis/llm_extractor.py`, `src/analysis/issuer_fallback.py` и compatibility-layer `src/analysis/confidence.py` синхронизированы с V2 shape. Валидация зелёная: semantic/consumer backend slice (`tests/test_extractor_semantics.py`, `tests/test_extractor_ranking_v2.py`, `tests/test_extractor_evidence_emission.py`, `tests/test_confidence_score.py`, `tests/test_confidence_properties.py`, `tests/test_tasks.py`, `tests/test_api.py`, `tests/test_llm_extractor.py`, `tests/test_llm_extractor_properties.py`, `tests/test_issuer_fallback.py`) → `152 passed`; extractor regression slice (`tests/test_pdf_extractor_facade.py`, `tests/test_analysis_pdf_extractor.py`, `tests/test_pdf_extractor.py`, `tests/test_pdf_regression_corpus.py`) → `126 passed`; frontend targeted vitest slice → `18 passed`; `npm --prefix frontend run build` → success; `black --check` и `isort --check-only` по новым semantic files/tests → clean. Residual note: root `.gitignore` по-прежнему содержит `test_*.py`, поэтому новые tests под `tests/` нужно force-add’ить (`git add -f ...`) или позже сужать ignore-rule отдельно.
- **Текущая агентская сессия**: 2026-04-04 — завершён следующий extractor cleanup-pass: из `src/analysis/extractor/legacy_helpers.py` физически удалён historical monolithic parse-body, который после staged refactor уже не был runtime source of truth и держался только как compat ballast под поздним shadowing. В файле оставлены только thin compatibility entrypoints `parse_financial_statements_with_metadata()` и `parse_financial_statements()`, которые late-bound делегируют в `src/analysis/extractor/pipeline.py`; добавлен regression-test в `tests/test_pdf_extractor_facade.py`, который жёстко проверяет, что в `legacy_helpers.py` остаётся ровно одно определение каждого parse-entrypoint. Closure verification зелёная: таргетный delegation/assertion slice проходит (`2 passed`), formatter/import checks чистые, а полный extractor closure set после физического удаления giant-body остаётся зелёным — `274 passed, 1 skipped`. Residual debt ещё есть, но уже существенно меньше: `legacy_helpers.py` остаётся compat/internal glue-модулем, однако больше не содержит второй исторической оркестрации parse pipeline.
- **Текущая агентская сессия**: 2026-04-03 — выполнен следующий extractor refactor pass по safe-SOLID плану. `src/analysis/extractor/pipeline.py` теперь стал реальным staged orchestrator вместо thin pass-through: введены typed stage containers `DocumentSignals`, `ExtractorContext`, `RawMetricCandidate`, `RawCandidates`; table/text collection вынесены в `tables.py` и `text_extraction.py`; derivation и post-parse guardrails вынесены в `guardrails.py`; precedence для typed raw-кандидатов централизована в `ranking.py`. Дополнительно зафиксирован новый characterization harness в `tests/test_pdf_extractor_facade.py`: он проверяет stage-type contract, explicit stage order, legacy P&L sanity conflict resolution и то, что `legacy_helpers.parse_financial_statements_with_metadata()` теперь runtime-делегирует в staged pipeline. В ходе parity-pass пойманы и закрыты три скрытых regression-risk’а: 1) IFRS exact-pass нельзя запускать на двухколоночных строках, иначе narrative noise вроде `Revenue growth 10,000` ошибочно становится `table_exact`; 2) garbled keyword matching обязан использовать `garbled_kw.lower()`, иначе битая кириллица ломается после `label.lower()`; 3) `_apply_form_like_pnl_sanity()` должен сохранять весь legacy fallback/conflict-drop path, а не только branch с двумя code-candidates. Closure verification зелёная: `python -m pytest tests/test_pdf_extractor_facade.py tests/test_analysis_pdf_extractor.py tests/test_pdf_extractor.py tests/test_confidence_score.py tests/test_confidence_properties.py tests/test_issuer_fallback.py tests/test_pdf_regression_corpus.py tests/test_pdf_real_fixtures.py tests/test_llm_extractor.py tests/test_llm_extractor_properties.py tests/test_tasks.py tests/test_api.py tests/test_scoring.py tests/test_pdf_local_magnit_regression.py -q` → `273 passed, 1 skipped`; `black --check` и `isort --check-only` по затронутым extractor-модулям и façade tests чистые. Residual debt уменьшен, но не нулевой: historical monolithic parse-body всё ещё физически остаётся в `legacy_helpers.py` как временный compat ballast, хотя runtime entrypoint уже переведён на новый pipeline.
- **Текущая агентская сессия**: 2026-04-03 — выполнен safe-split refactor `src/analysis/pdf_extractor.py` без намеренного изменения extraction behavior. Введён внутренний пакет `src/analysis/extractor/` с модулями `types`, `ranking`, `rules`, `ocr`, `tables`, `text_extraction`, `guardrails`, `pipeline`, а сам `pdf_extractor.py` сокращён до thin backward-compatible facade. Критичный compatibility-risk закрыт тестами и façade design: monkeypatch-sensitive surface (`camelot`, `PyPDF2`, `pytesseract`, `convert_from_path`, `TESSERACT_AVAILABLE`, `MAX_OCR_PAGES`, layout OCR helpers) оставлен на старом модуле, но реальные public entrypoints теперь живут в новых внутренних модулях. Добавлен `tests/test_pdf_extractor_facade.py`, который фиксирует re-export contract и проверяет, что `determine_source / apply_confidence_filter / is_scanned_pdf / extract_text_from_scanned / extract_tables / extract_metrics_regex / parse_financial_statements_with_metadata` уже объявлены в `src/analysis/extractor/*`, а не в façade. Regression verification зелёная на широком extractor-consumer surface: `tests/test_pdf_extractor_facade.py`, `tests/test_analysis_pdf_extractor.py`, `tests/test_pdf_extractor.py`, `tests/test_confidence_score.py`, `tests/test_confidence_properties.py`, `tests/test_issuer_fallback.py`, `tests/test_pdf_regression_corpus.py`, `tests/test_pdf_real_fixtures.py`, `tests/test_tasks.py`, `tests/test_scoring.py`, `tests/test_api.py`, `tests/test_llm_extractor.py`, `tests/test_llm_extractor_properties.py` → все passed; `tests/test_pdf_local_magnit_regression.py` → skipped по fixture gate. Formatter gate тоже закрыт: `black --check` и `isort --check-only` по façade/new package/tests чистые. Residual architectural note: `legacy_helpers.py` пока остаётся большим internal compatibility-модулем, но product-facing import surface и orchestration уже вынесены из старого god-file.
- **Текущая агентская сессия**: 2026-04-03 — закрыт следующий CI compose/runtime blocker на build surface. После secretless-fix для `db`/`db_test` GitHub Actions впервые довёл stack до реального runtime orchestration issue: оба Postgres-контейнера и backend-migrations уже были зелёными, но `frontend` падал на nginx upstream resolution (`host not found in upstream "backend"`), а build-job затем зависал в ожидании healthy state. Root cause оказался составным и лежал в `docker-compose.ci.yml` + `ci.yml`: CI backend по-прежнему собирался из root `Dockerfile` с `ENTRYPOINT ["./entrypoint.sh"]`, то есть как migration-only container вместо long-running API runtime, frontend стартовал без `depends_on: backend: service_healthy`, а workflow пытался ожидать весь stack через ручной poll-loop, хотя compose runtime здесь уже требует раздельного запуска one-shot migration job и long-running сервисов. Пакет исправлен системно: `docker-compose.ci.yml` переведён на `Dockerfile.backend` для `backend`, добавлен `backend-migrate` service с `entrypoint.sh`, backend теперь ждёт `service_completed_successfully`, frontend использует `VITE_API_BASE: /api` и ждёт healthy backend; в `.github/workflows/ci.yml` build-step собирает `backend frontend backend-migrate`, а startup разбит на три безопасные фазы: `db/db_test` через `up -d --wait`, затем `backend-migrate` через `up --no-deps --exit-code-from`, затем `backend/frontend` через `up -d --wait`. В `tests/test_github_workflows.py` добавлены regression-tests на runtime Dockerfile, migration job, frontend/backend health ordering и explicit migration phase в workflow. Closure verification зелёная: `python -m pytest tests/test_github_workflows.py -q` → `19 passed`, `python -m black --check tests/test_github_workflows.py` → unchanged, `python -m isort --profile black --check-only tests/test_github_workflows.py` → clean, `git diff --check -- .github/workflows/ci.yml docker-compose.ci.yml tests/test_github_workflows.py` → чисто, `$env:CI_DB_PASSWORD='neofin_ci_password'; docker compose -f docker-compose.ci.yml config` → parse OK. Residual note: полный local `docker compose up ... --wait` smoke в этой сессии не прогнан, потому что локальный Docker daemon выключен (`dockerDesktopLinuxEngine` not found).
- **Текущая агентская сессия**: 2026-04-03 — закрыт следующий CI compose-blocker на build/runtime surface. После фиксов `docker compose`, `.gitignore` и frontend component tracking GitHub Actions дошёл до `docker compose up -d`, где оба Postgres-контейнера (`db`, `db_test`) умирали ещё до backend startup. Root cause оказался двойным и лежал в `docker-compose.ci.yml`/`ci.yml`: compose-файл продолжал требовать `${DB_PASSWORD}` для ephemeral CI Postgres, а build-job тянул `DB_PASSWORD` из repository secret; одновременно backend service в compose вообще не получал `DATABASE_URL` и `TEST_DATABASE_URL`, так что даже после оживления БД следующим падением стал бы backend runtime. Пакет исправлен системно: `docker-compose.ci.yml` переведён на `${CI_DB_PASSWORD}` для `db` и `db_test`, backend получил реальные `DATABASE_URL`/`TEST_DATABASE_URL` и runtime env passthrough для optional AI/CORS surface, а из `ci.yml` build/start step env убрана зависимость от `secrets.DB_PASSWORD`. В `tests/test_github_workflows.py` добавлены regression-tests на compose password source, отсутствие repository-secret DB password в build-job и наличие runtime DB URLs у backend service. Closure verification зелёная: `python -m pytest tests/test_github_workflows.py -q` → `15 passed`, `python -m black --check tests/test_github_workflows.py` → unchanged, `python -m isort --profile black --check-only tests/test_github_workflows.py` → clean, `git diff --check -- .github/workflows/ci.yml docker-compose.ci.yml tests/test_github_workflows.py` → чисто.
- **Текущая агентская сессия**: 2026-04-02 — после фикса `.gitignore`/missing frontend components PR вскрыл ещё один чисто formatter-only blocker: `CI/CD Pipeline / Code Linting` падал на `isort` в `tests/test_github_workflows.py`. Root cause узкий: после серии правок в import-блок файла `import subprocess` остался в порядке, который устраивает `black`, но не устраивает `isort --profile black --check-only`. Файл выровнен через `python -m isort --profile black tests/test_github_workflows.py`; свежая проверка: `python -m isort --profile black --check-only tests/test_github_workflows.py` → clean, `python -m black --check tests/test_github_workflows.py` → unchanged, `python -m pytest tests/test_github_workflows.py -q` → `12 passed`. Следующий шаг: commit/push этого formatter-only fix, чтобы PR пересчитал lint-check уже без import-order drift.
- **Текущая агентская сессия**: 2026-04-02 — найден и закрыт следующий frontend build-blocker в CI. После починки `docker compose` Docker build дошёл до настоящей product-surface ошибки: `vite build` в контейнере падал с `Could not resolve "./components/Layout" from "src/App.tsx"`. Root cause оказался не в Vite и не в Docker context, а в корневом `.gitignore`: шаблон `com*`, добавленный под Windows reserved names, рекурсивно игнорировал весь `frontend/src/components/*`, поэтому локально файлы существовали, но в git/CI их не было. Safe fix выполнен в два слоя: `.gitignore` сужен до root-only `COM1..COM9`/`LPT1..LPT9`, в `tests/test_github_workflows.py` добавлен regression-test, что critical frontend components больше не скрываются git’ом, а missing component surface (`Layout`, `ProtectedRoute`, `AppFooter`, `ConfidenceBadge`, `TrendChart`, `AiProviderMenu` и component tests) подготовлен к добавлению в индекс. Свежая верификация: `python -m pytest tests/test_github_workflows.py -q` → `12 passed`, `npm --prefix frontend run build` → success, `npm --prefix frontend run lint` → success, `npm --prefix frontend run test -- src/components/__tests__/AiProviderMenu.test.tsx src/components/__tests__/ConfidenceBadge.test.tsx src/components/__tests__/DetailedMetricsCard.test.tsx src/components/__tests__/Layout.test.tsx src/components/__tests__/ScoreInsightsCard.test.tsx src/components/__tests__/TrendChart.test.tsx` → `31 passed`.
- **Текущая агентская сессия**: 2026-04-02 — закрыт ещё один GitHub Actions blocker в build-job: на `ubuntu-latest` workflow использовал legacy `docker-compose`, из-за чего шаги `Build Docker image` и `Cleanup containers` падали с `docker-compose: command not found`. Root cause подтверждён узким regression-test в `tests/test_github_workflows.py`, после чего `ci.yml` переведён на Compose V2 CLI `docker compose` для `build/up/ps/logs/down`. Верификация зелёная: `python -m pytest tests/test_github_workflows.py -q` → `11 passed`, `python -m black --check tests/test_github_workflows.py` → `unchanged`, `Select-String -Path .github/workflows/ci.yml -Pattern 'docker-compose -f'` → пусто, `git diff --check -- .github/workflows/ci.yml tests/test_github_workflows.py` → чисто. Следующий шаг: commit/push этого CI-only фикса и дождаться пересчёта build-check в PR.
- **Текущая агентская сессия**: 2026-04-02 — закрыт ещё один точечный PR-check blocker: `Code Linting` падал не на логике workflow-regression тестов, а на чистом formatter drift в `tests/test_github_workflows.py`. Root cause узкий: после добавления длинного имени `test_ci_pipeline_generates_coverage_artifact_without_duplicating_fail_under_gate` файл не был прогнан через `black`, и CI честно требовал перенос сигнатуры функции. Файл отформатирован точечно командой `python -m black tests/test_github_workflows.py`, после чего локальная верификация зелёная: `python -m black --check tests/test_github_workflows.py` → `unchanged`, `python -m pytest tests/test_github_workflows.py -q` → `10 passed`, `git diff --check -- tests/test_github_workflows.py` → чисто. Следующий шаг: commit/push этого минимального formatter-fix, чтобы PR пересчитал lint-check.
- **Текущая агентская сессия**: 2026-04-02 — финальный CI red-check найден и закрыт на уровне orchestration, а не кода продукта. Через GitHub API подтверждено, что на свежем `head_sha=74391bc` уже зелёный `Code Quality`, а красным остаётся только `CI/CD Pipeline / Run Tests`, причём падает именно дублирующий шаг `Run coverage check`. Локальная проверка exact-командой из `ci.yml` с текущими `--ignore` показывает `83.24%`, но GitHub-run на этом шаге всё равно считал только `73.58%`; это означает не продуктовый регресс, а несогласованный duplicate gate между двумя workflow. Для починки `ci.yml` coverage-step переведён из blocking gate в artifact-only step `Generate coverage artifact` без `--cov-fail-under`, а единственным авторитетным coverage-threshold остаётся `Code Quality` workflow. В `tests/test_github_workflows.py` добавлен regression-test, что `CI/CD Pipeline` больше не дублирует fail-under gate. Свежая проверка: `python -m pytest tests/test_github_workflows.py -q` → `10 passed`, `git diff --check -- .github/workflows/ci.yml tests/test_github_workflows.py` → чисто. Следующий шаг: commit/push workflow-alignment fix и дождаться финального зелёного пересчёта PR.
- **Текущая агентская сессия**: 2026-04-02 — ещё один CI-blocker закрыт: после фикса `QWEN_API_URL=''` GitHub начал падать уже на `ModuleNotFoundError: No module named 'hypothesis'` при сборе property-based тестов (`tests/test_analyses_router.py`, `tests/test_confidence_properties.py`, `tests/test_crud_analyses.py`, `tests/test_masking.py`, `tests/test_multi_analysis_router.py`, `tests/test_qwen_regression_preservation.py`). Root cause оказался простым и точечным: workflow честно ставит `requirements-dev.txt`, но в самом файле не было `hypothesis`, хотя локально библиотека была установлена и потому проблема не проявлялась. В `requirements-dev.txt` добавлен `hypothesis~=6.151.0`, а `tests/test_github_workflows.py` получил regression-guard на наличие Hypothesis в dev requirements. Свежая верификация: `python -m pytest tests/test_github_workflows.py -q` → `9 passed`; `python -m pytest tests/test_analyses_router.py tests/test_confidence_properties.py tests/test_crud_analyses.py tests/test_masking.py tests/test_multi_analysis_router.py tests/test_qwen_regression_preservation.py -q` → `68 passed`; `python -m pytest tests --collect-only -q` → `900 items / 1 skipped`; полный suite `python -m pytest -q --maxfail=120` → `895 passed, 4 skipped, 2 xfailed`; полный coverage-run `python -m pytest --cov=src --cov-report=term --cov-report=xml:.tmp/backend-coverage-post-hypothesis.xml -q` → `83.24%`. Следующий шаг: commit/push этого dependency-fix пакета и дождаться нового пересчёта checks в PR.
- **Текущая агентская сессия**: 2026-04-02 — найден и закрыт ещё один реальный CI-blocker после runner-network fix: `pytest` collection и `alembic env` падали на import-time `AppSettings`, если в GitHub Actions `QWEN_API_URL` приходил как пустая строка `''`. Корень оказался в `src/models/settings.py`: URL validator для optional полей трактовал пустую строку как невалидный URL вместо “отсутствующего значения”. Исправление выполнено точечно и без расширения blast radius: `validate_urls()` теперь делает `strip()` и нормализует empty/whitespace-only values в `None`, после чего валидирует только непустые значения. Тестовое покрытие обновлено в `tests/test_models_settings.py` и `tests/test_settings_coverage.py`. Свежая верификация: `python -m pytest tests/test_models_settings.py tests/test_settings_coverage.py tests/test_agent.py tests/test_github_workflows.py -q` → `81 passed, 1 skipped`; отдельная CI-like проверка с пустыми env проходит: `$env:QWEN_API_URL=''; $env:QWEN_API_KEY=''; python -m pytest tests --collect-only -q` → `899 tests collected`. Следующий шаг: закоммитить и запушить маленький settings-fix commit, затем дождаться пересчёта GitHub checks.
- **Текущая агентская сессия**: 2026-04-02 — PR checks remediation доведён до второго, уже network-aware fix-pass. После первого push `fa513a7` GitHub перевёл `Type Checking` в зелёный, но новые runs всё ещё падали на трёх конкретных местах: `Code Linting` валился на `scripts/`, `Run Tests` — на `Run database migrations`, `Test Coverage` — на `Run tests with coverage`. Root cause подтверждён через GitHub Actions docs в Context7 и локальные regression-тесты: runner-based jobs не могут ходить в service containers по label (`postgres-main`, `postgres-test`) без `ports`, им нужен `localhost` + явный port mapping. Для фикса добавлены два новых regression-теста в `tests/test_github_workflows.py`, `ci.yml` и `code-quality.yml` переведены на `ports` + `localhost`, а `scripts/admin_cleanup.py`, `scripts/runtime_recover.py`, `scripts/demo_smoke.py`, `scripts/validate_env.py` синхронизированы с black/isort baseline. Свежая верификация: `python -m pytest tests/test_github_workflows.py -q` → `8 passed`, `python -m black --check src tests scripts` → `OK`, `python -m isort --profile black --check-only src tests scripts` → `OK`, `python -m pytest tests/test_github_workflows.py tests/test_ratios.py tests/test_scoring.py tests/test_pdf_extractor.py tests/test_api.py -q` → `91 passed`, `python -m pytest --collect-only -q` → `897 items / 1 skipped`, полный suite `python -m pytest -q --maxfail=120` → `892 passed, 4 skipped, 2 xfailed`. Следующий шаг: закоммитить второй CI-fix пакет и дождаться финального пересчёта checks в PR.
- **Текущая агентская сессия**: 2026-04-02 — PR checks remediation почти закрыт. После открытия PR `#1` были подтверждены три независимых root cause: `Code Linting` падал на реальном formatter drift (`black` + `isort/black profile mismatch`), `Run Tests` и `Test Coverage` валились на `Initialize containers` из-за зависимости ephemeral Postgres services от `secrets.DB_PASSWORD`, а `Type Checking` пытался проверять весь `src` в режиме, которому текущий репозиторий не соответствует. В итоге: `.github/workflows/ci.yml` переведён на secretless `CI_DB_PASSWORD` и `isort --profile black`, `.github/workflows/code-quality.yml` переведён на secretless DB setup и реалистичный typed-contract gate (`schemas.py`, `requests.py`, `database/user.py`, `circuit_breaker.py` через `MYPYPATH=. mypy --namespace-packages --explicit-package-bases --follow-imports=silent --ignore-missing-imports`), добавлен regression-test `tests/test_github_workflows.py`, а repo приведён к текущему formatter baseline через `isort --profile black src tests` + `black src tests`. Свежая верификация: `black --check src tests` → OK, `isort --profile black --check-only src tests` → OK, `pytest tests/test_github_workflows.py -q` → `6 passed`, `pytest tests/test_github_workflows.py tests/test_ratios.py tests/test_scoring.py tests/test_pdf_extractor.py tests/test_api.py -q` → `89 passed`, полный suite `pytest -q --maxfail=120` → `890 passed, 4 skipped, 2 xfailed`, `pytest --collect-only -q` → `895 items / 1 skipped`, coverage JSON обновлён до `83.21% lines`, `84.89% statements`, `78.62% branches`.
- **Текущая агентская сессия**: 2026-04-02 — post-PR checks blocker закрыт. После открытия PR `#1` выяснилось, что merge-blocker сидит не в коде продукта и не в конфликтах ветки, а в двух broken GitHub Actions workflows: `ci.yml` содержал неверное job-level поле `environment` вместо `env` для build flags, а `code-quality.yml` имел YAML-invalid heredoc в шаге coverage threshold. Для фикса добавлен regression-test `tests/test_github_workflows.py`, который проверяет парсинг всех `.github/workflows/*.yml` и запрещает misuse `environment` в `ci.yml build`. Верификация свежая: `python -m pytest tests/test_github_workflows.py -q` → `2 passed`, `yaml.safe_load(...)` успешно парсит оба workflow, `git diff --check -- .github/workflows/ci.yml .github/workflows/code-quality.yml tests/test_github_workflows.py` чистый. После этого ветка готова к дополнительному push, а PR может перейти к нормальному пересчёту checks на GitHub.
- **Текущая релизная сессия**: 2026-04-02 — release-finalization пакет доведён до verify-ready состояния. Публичные dense docs больше не переписываются “с нуля”: `README.md`, `docs/API.md`, `docs/ARCHITECTURE.md`, `docs/CONFIGURATION.md`, `docs/BUSINESS_MODEL.md`, `frontend/README.md`, contest docs и roadmap синхронизированы поверх исходной структуры. Cleanup публичного пакета завершён: удалены internal `report/backlog/plan` файлы, `.tmp/` исключён из release surface, frontend manifest очищен от `@google/genai`, `dotenv`, `express`, `@types/express`. Свежая верификация на финальном дереве: `npm --prefix frontend run lint` → `OK`, `npm --prefix frontend run coverage` → `96 passed` (`65.05% lines`, `63.99% statements`, `68.88% functions`, `53.21% branches`), `python -m pytest --collect-only -q` → `889 items / 1 skipped`, `python -m pytest -q --maxfail=120` → `884 passed, 4 skipped, 2 xfailed`, `python -m pytest --cov=src --cov-report=json:.tmp/backend-coverage.json -q` → `83.23% statements/lines`, `78.67% branches`. Ветка готова к staging/commit/push в `codex/all-metrics-extraction`.
- **Текущая агентская сессия**: 2026-04-02 — ошибка стратегии публичной документации исправлена. Для `README.md`, `docs/ARCHITECTURE.md` и `docs/BUSINESS_MODEL.md` восстановлена плотная базовая версия из `main`, после чего поверх неё выполнена только точечная синхронизация под текущий релиз: бренд `НеоФин.Документы / НеоФин.Контур`, truthful scoring и benchmark’ы `generic / retail_demo`, `score.methodology`, `ai_runtime`, `issuer_fallback`, baseline тестов/coverage, а также актуальная UI-формулировка `Надёжно извлечено: N показателей`. Ключевой process-rule зафиксирован: для dense public docs в этом проекте safe path = `restore-from-main + surgical sync`, а не rewrite “с нуля”.
- **Текущая агентская сессия**: 2026-04-02 — `test hygiene / harness stabilization` закрыт. После предыдущего coverage-среза, где backend показывал десятки падений, основной root cause оказался не в продуктовой логике, а в смеси stale regression tests и нестабильного test harness вокруг `pytest-asyncio + asyncpg + TestClient`. В `tests/conftest.py` тестовая Postgres schema переведена на function-scoped lifecycle, `search_path` больше не прокидывается через URL `options` (что ломало `asyncpg`), а задаётся через `connect_args={"server_settings": {"search_path": schema}}`; monkeypatch в `client/db_session` теперь идёт по актуальному `get_session_maker()` contract вместо мёртвого `crud.AsyncSessionLocal`. Устаревшие/stale тесты синхронизированы под текущий runtime: `tests/test_llm_extractor_properties.py` переведён на structured `{"metrics":[...]}` payload, `asyncio.run(...)` и `LlmExtractionRunResult`, `tests/test_qwen_regression_preservation_2.py` учитывает dual leverage с `short_term_borrowings/long_term_borrowings`, а `tests/test_e2e.py` и `tests/test_frontend_e2e.py` больше не тянут реальную async DB через `TestClient`, а честно мокают route-level CRUD/dispatch boundary, оставляя саму БД покрытой отдельно в `tests/test_db_integration.py`. Verification теперь зелёная не частично, а полностью: целевой red-slice `tests/test_qwen_regression_fixes.py tests/test_qwen_regression_integration.py tests/test_qwen_regression_preservation.py tests/test_qwen_regression_preservation_2.py tests/test_settings_coverage.py tests/test_core_gigachat_agent.py tests/test_llm_extractor.py tests/test_llm_extractor_properties.py tests/test_benchmarks.py tests/test_db_integration.py tests/test_e2e.py tests/test_frontend_e2e.py -q` → `179 passed, 1 skipped`; полный backend suite `python -m pytest -q --maxfail=120` → `884 passed, 4 skipped, 2 xfailed, 1 warning`. Явный local `code_review` pass выполнен: `git diff --check` по изменённым тестовым файлам чистый, blocking findings не осталось.
- **Текущая продуктовая сессия**: 2026-04-02 — frontend rebrand/truthfulness pack для `НеоФин.Документы` доведён до рабочего состояния. Пользовательский surface переведён с legacy `NeoFin AI` на новый бренд `НеоФин.Документы` / `НеоФин.Контур`: в `frontend/src/constants/branding.ts` вынесены канонические brand constants, `frontend/src/components/Layout.tsx` и `frontend/src/pages/Auth.tsx` получили новый центрированный бренд, русский navigation shell и legal footer `НеоФин. Все права защищены, 2026.`, а `frontend/src/pages/Dashboard.tsx` — обновлённый hero, продуктовый description block и default provider policy `prefer ollama if available`. Отдельно закрыты две user-facing ложности: `frontend/src/components/report/DetailedMetricsCard.tsx` больше не показывает абсурдное `X из 15`, а `frontend/src/components/report/ScoreInsightsCard.tsx` теперь рендерит AI-copy только по новому backend truth-source `data.ai_runtime`, а не по косвенной эвристике `nlp`. На backend для этого синхронизированы `src/analysis/nlp_analysis.py`, `src/tasks.py`, `src/models/schemas.py`, `frontend/src/api/interfaces.ts`: payload одиночного анализа теперь содержит `ai_runtime.requested_provider`, `effective_provider`, `status`, `reason_code`, и live smoke через `http://127.0.0.1/api/*` уже подтвердил честное поведение на свежем Docker runtime — артефакт `.tmp/frontend_rebrand_smoke_cloudflare.json` содержит `ai_runtime={"status":"failed","reason_code":"provider_error","effective_provider":"ollama","requested_provider":"ollama"}` при `status=completed`, так что UI больше не обязан гадать, “доступен AI или нет”. Docs sync тоже закрыт: `docs/API.md` и `docs/CONFIGURATION.md` обновлены под бренд `НеоФин.Документы` и новый `ai_runtime` contract. Проверка зелёная: `npm --prefix frontend run test -- src/components/__tests__/Layout.test.tsx src/components/__tests__/DetailedMetricsCard.test.tsx src/components/__tests__/ScoreInsightsCard.test.tsx src/pages/__tests__/Auth.test.tsx src/pages/__tests__/Dashboard.test.tsx src/pages/__tests__/SettingsPage.test.tsx src/pages/__tests__/DetailedReport.test.tsx` → `40 passed`, `python -m pytest tests/test_api.py tests/test_tasks.py tests/test_nlp_analysis.py -q` → `55 passed`, `git diff --check` без blocking errors, а визуальный auth gut-check сохранён в `.tmp/auth_page_20260402.png`.
- **Текущая агентская сессия**: 2026-04-02 — математический roadmap вынесен в отдельный продуктовый документ [docs/MATH_ROADMAP.md](/E:/neo-fin-ai/docs/MATH_ROADMAP.md) и связан с [docs/ROADMAP.md](/E:/neo-fin-ai/docs/ROADMAP.md). Документ фиксирует, что следующий математический контур NeoFin AI должен идти в порядке `P0 -> P1 -> P2 -> P3 -> P4`: `Formula Correctness & Period Truth`, `Debt/Lease & Cash-Flow Risk Layer`, `Provenance & Truth Registry`, `Empirical Benchmark Calibration`, `Confidence/Uncertainty & Analyst Mode`. Ключевой смысл roadmap: truthfulness выше “красивого” score, а развитие математики должно идти от correctness и period/provenance discipline к sector calibration и confidence-layer. Быстрый docs-pass выполнен: `git diff --check -- docs/MATH_ROADMAP.md docs/ROADMAP.md` чистый.
- **Текущая продуктовая сессия**: 2026-04-02 — `Magnit formula/issuer-fallback repair` завершён и подтверждён на живом Docker runtime. Пакет truthful retail scoring теперь не только зелёный в unit/regression слоях, но и реально совпадает с proxy-level `/api/*` на 4 реальных PDF Магнита. В `src/analysis/ratios.py` закреплены corrected `interest_coverage = EBIT / abs(interest_expense)` и dual leverage metrics (`financial_leverage_total`, `financial_leverage_debt_only`), в `src/analysis/scoring.py` scoring-active leverage alias для retail переключается на `debt_only`, а `score.methodology` расширен полями `leverage_basis`, `ifrs16_adjusted`, `adjustments`, `peer_context`. `src/analysis/pdf_extractor.py` теперь извлекает `short_term_borrowings`, `long_term_borrowings`, `short_term_lease_liabilities`, `long_term_lease_liabilities`, не путает EBITDA с nearby PBT/finance rows и сохраняет scanned Q1 revenue без ложного `missing_core:revenue`. Добавлен repo-versioned `src/analysis/issuer_fallback.py` для Magnit H1 2025 (`ebitda`, `interest_expense`, `net_profit`) с `source='issuer_fallback'`, а публичный контракт синхронизирован через `src/models/schemas.py`, `frontend/src/api/interfaces.ts`, `frontend/src/components/report/ScoreInsightsCard.tsx`, `docs/API.md`, `docs/CONFIGURATION.md`. Самое важное live-proof: после rebuild `docker compose up -d --build backend worker` свежий proxy-smoke на `http://127.0.0.1/api/*` сохранён в `.tmp/magnit_proxy_e2e_results_20260402_truthful_scoring.json` и полностью зелёный — `magnit_2025_q1_scanned` → `57.78/medium/0.75`, `magnit_2022_ifrs` → `67.04/medium/0.95`, `magnit_2023_ifrs` → `70.35/medium/0.95`, `magnit_2025_h1_ifrs` → `55.06/medium/0.95`. Closure validation: `tests/test_issuer_fallback.py tests/test_scoring.py` → `17 passed`, `RUN_LOCAL_PDF_REGRESSION=1 python -m pytest tests/test_pdf_local_magnit_regression.py -q` → `6 passed`, `npm --prefix frontend run lint` → `OK`, `npm --prefix frontend run test -- src/components/__tests__/ScoreInsightsCard.test.tsx src/hooks/__tests__/useAnalysisHistory.test.ts` → `16 passed`. Явный local `code_review` pass выполнен: блокирующих замечаний нет.
- **Текущая продуктовая сессия**: 2026-04-01 — truthful retail-aware scoring calibration завершён и подтверждён на live runtime. Скоринговый пайплайн больше не жёстко сидит на `generic`: `src/analysis/scoring.py` получил document-aware auto-resolver с `benchmark_profile` (`generic` / `retail_demo`), `period_basis` (`reported` / `annualized_q1` / `annualized_h1`), прозрачными `reasons` и `guardrails`, а `src/tasks.py` теперь передаёт в scoring не только метрики, но и контекст документа (`filename`, `text`). Для interim periods annualization применяется только к P&L/activity inputs (`revenue`, `net_profit`, `ebitda`, `ebit`, `interest_expense`, `cost_of_goods_sold`), баланс остаётся нетронутым. Публичный контракт расширен честно и синхронно: `src/models/schemas.py` и `frontend/src/api/interfaces.ts` теперь содержат `score.methodology` / `score_methodology`, а `frontend/src/components/report/ScoreInsightsCard.tsx` рендерит badges `Retail benchmark`, `Annualized H1/Q1`, `Data-quality cap` и объясняет, почему score именно такой. Документация синхронизирована (`docs/API.md`, `docs/CONFIGURATION.md`, `.env.example`). Closure validation зелёная: `tests/test_pdf_extractor.py tests/test_pdf_regression_corpus.py tests/test_pdf_real_fixtures.py tests/test_scoring.py tests/test_analysis_scoring.py tests/test_tasks.py tests/test_api.py tests/test_multi_analysis_router.py -q` → `168 passed`; `RUN_LOCAL_PDF_REGRESSION=1 python -m pytest tests/test_pdf_local_magnit_regression.py -q` → `6 passed`; `npm --prefix frontend run lint` → `OK`; `npm --prefix frontend run test -- src/components/__tests__/ScoreInsightsCard.test.tsx src/pages/__tests__/Dashboard.test.tsx` → `5 passed`. Самое важное — свежий proxy-level smoke на 4 реальных PDF Магнита через `http://127.0.0.1/api/*` подтверждён после rebuild контейнеров и сохранён в `.tmp/magnit_proxy_e2e_results_20260401_retail_scoring.json`: `magnit_2025_q1_scanned` → `39.99/high/0.65` (`retail_demo + reported + missing_core:revenue`), `magnit_2022_ifrs` → `60.78/medium/0.95`, `magnit_2023_ifrs` → `66.08/medium/0.95`, `magnit_2025_h1_ifrs` → `33.99/critical/0.95` (`retail_demo + annualized_h1`). Явный local `code_review` pass выполнен: блокирующих замечаний по пакету нет.
- **Текущая продуктовая сессия**: 2026-04-01 — Docker/frontend runtime и свежий Magnit acceptance доведены до честного зелёного состояния. Корень контейнерной проблемы был не в самом nginx и не во frontend build, а в ложном self-health probe: `frontend/Dockerfile` и `frontend/Dockerfile.frontend` использовали `wget http://localhost/`, из-за чего контейнер регулярно уходил в `unhealthy` с `Connection refused` при полностью рабочем UI. Через Context7 сверена актуальная модель `HEALTHCHECK CMD` для HTTP-проверок; оба Dockerfile переведены на loopback-probe `http://127.0.0.1/`, а `tests/test_docker_runtime.py` получил regression coverage на оба образа. Для UI добавлен regression-test `frontend/src/pages/__tests__/Dashboard.test.tsx` на рендер provider selector в upload-секции; текущий runtime path также подтверждён через `/api/system/ai/providers` по фронтовому proxy (`auto,gigachat,huggingface,ollama`). Свежая verification evidence: `docker compose ps` показывает `frontend` как `healthy`, `GET http://127.0.0.1/` → `200`, `GET http://127.0.0.1/api/system/health` → `200`, `tests/test_docker_runtime.py` → `3 passed`, `npm --prefix frontend run lint` → `OK`, `npm --prefix frontend run test -- src/components/__tests__/AiProviderMenu.test.tsx src/pages/__tests__/Dashboard.test.tsx` → `3 passed`. Самый важный closure gate закрыт live-проверкой на 4 реальных PDF Магнита через фронтовой proxy `http://127.0.0.1/api/*`: результаты сохранены в `.tmp/magnit_proxy_e2e_results_20260401.json`, все кейсы `ok=true`, метрики совпали с manifest, а scoring тоже совпал с живым baseline: `magnit_2025_q1_scanned` → `39.99/high/0.65`, `magnit_2022_ifrs` → `44.92/high/0.95`, `magnit_2023_ifrs` → `47.82/high/0.95`, `magnit_2025_h1_ifrs` → `23.82/critical/0.95`. Дополнительно `python scripts/demo_smoke.py --base-url http://127.0.0.1 --api-prefix /api --api-key dev-key-123 --scenario text_single --scenario scanned_single --scenario multi_period_magnit` завершился успешно. Явный local `code_review` pass выполнен: блокирующих замечаний по пакету нет.
- **Текущая агентская сессия**: 2026-04-01 — local `Ollama extraction` на `qwen3.5:9b` доведён до безопасного runtime-state на real PDFs. Через Context7 подтверждён актуальный structured-output path Ollama (`format=<JSON Schema>`, `stream=false`, `num_predict` как output cap), после чего extraction-контур усилен в двух местах: `src/analysis/llm_extractor.py` теперь держит строгий wire-shape `{"metrics":[...]}`, умеет делать one-shot retry для invalid schema и salvage-ить complete metric objects из truncated JSON; `src/tasks.py` больше не даёт `llm` перетирать сильные deterministic значения и использует guarded merge, где `llm` заполняет missing/derived поля или заменяет только слабый fallback. Полный verification pack зелёный: `tests/test_core_ai_service.py tests/test_llm_extractor.py tests/test_nlp_analysis.py tests/test_recommendations.py tests/test_tasks.py` → `139 passed`; `tests/test_pdf_extractor.py tests/test_pdf_regression_corpus.py tests/test_pdf_real_fixtures.py` → `68 passed`; `RUN_LOCAL_PDF_REGRESSION=1 python -m pytest tests/test_pdf_local_magnit_regression.py -q` → `6 passed`; `tests/test_api.py tests/test_scoring.py tests/test_analysis_scoring.py` → `35 passed`. Live acceptance на свежем Ollama-only backend (`127.0.0.1:8001`) подтверждён на двух real PDFs: Cloudflare task `27990db1-0cf4-41ae-af5c-befc57779222` дал `LLM extraction completed: metrics=5`, salvage одного truncated chunk и безопасный вклад только в `cost_of_goods_sold`, при этом финальные ключевые метрики остались корректными (`revenue=1,296,745,000`, `equity=763,047,000`, `liabilities=1,996,720,000`); Magnit H1 2025 task `4fb07faf-0343-4a3a-ad95-a912ec759e26` дал `LLM extraction completed: metrics=7`, но все конфликтующие `llm`-значения были осознанно отвергнуты guardrail-merge, поэтому финальный payload снова совпадает с сильным deterministic parse (`revenue=1,673,223,617,000`, `equity=175,381,814,000`, `liabilities=1,486,023,626,000`, `short_term_liabilities=670,066,479,000`). Явный local `code_review` pass выполнен: блокирующих замечаний по diff нет.
- **Текущая агентская сессия**: 2026-04-01 — локальный `Ollama` runtime доведён до рабочего JSON-friendly состояния под ваш ПК. Железо подтверждено: `AMD Ryzen 5 2400G`, `25.7 GB RAM`, `NVIDIA GeForce RTX 2060 12 GB`; store моделей остаётся на `D:\work\ollama`, а user-level `OLLAMA_NO_GPU=1` удалён, после чего `ollama ps` показывает `qwen3.5:9b` на `100% GPU`. Через Context7 и официальные источники сверено, что `gpt-oss:20b` более силён по structured outputs, но для этой машины safer fit — `qwen3.5:9b` (`6.6 GB`) как более свежий и реалистичный локальный вариант на 2026-04-01. В коде закрыт реальный root cause Ollama-path: `src/core/ai_service.py` теперь пробрасывает `system`, `format`, принудительно ставит `think=false` и использует settings-backed `LLM_URL/LLM_MODEL`; `llm_extractor`, `nlp_analysis`, `recommendations` отправляют `format=json`. RED/GREEN validation зелёная: `tests/test_core_ai_service.py tests/test_llm_extractor.py tests/test_nlp_analysis.py tests/test_recommendations.py` → `104 passed`. Project-level проверки: raw `/api/generate` c `system+format=json+think=false` возвращает чистый JSON, inline `AIService/analyze_narrative/generate_recommendations` проходят без fallback, живой backend task `d24c1a5d-9bb3-4e48-83cc-b986be7d95b3` на `cloudflare_2023_annual_report.pdf` доходит до `completed`, а лог фиксирует `AI invocation started/completed (provider: ollama)` и `Generated 5 recommendations with data references`. Честный caveat: `qwen3.5:9b` всё ещё не держит exact schema `llm_extractor` и уходит в `Unexpected LLM response structure`, поэтому extraction-контур на local Ollama пока остаётся fallback-driven; NLP/recommendations уже рабочие, extraction schema — отдельный follow-up.
- **Текущая агентская сессия**: 2026-04-01 — локальный AI runtime переключён на `Ollama` без использования диска `C:`: подтверждено, что `OLLAMA_MODELS` задан на уровне `User/Machine` как `D:\work\ollama`, модель `llama3:latest` успешно установлена и видна через `ollama list`, а raw generate-check возвращает `OLLAMA_OK`. Backend перезапущен в `Ollama-only` режиме process-level overrides (`GIGACHAT_CLIENT_ID=none`, `GIGACHAT_CLIENT_SECRET=none`, `HF_TOKEN=none`, `QWEN_API_KEY=none`, `TESTING=0`, `TASK_RUNTIME=background`); startup log фиксирует `AI service configured with provider: ollama`, а project-level smoke на `tests/data/pdf_real_fixtures/cloudflare_2023_annual_report.pdf` дошёл до `status=completed` с живыми `AI invocation started/completed (provider: ollama)` в логе. Важный caveat: модель `llama3` на текущих strict JSON prompt'ах в `llm_extractor` и `nlp_analysis` отвечает свободным текстом, поэтому extraction/NLP контур деградирует в fallback (`LLM response is not valid JSON`, `Failed to parse AI response, returning fallback`), хотя сам runtime-switch и локальный provider path работают корректно.
- **Текущая продуктовая сессия**: 2026-04-01 — локальный frontend truthfulness fix для итоговой карточки завершён. Дополнительная end-to-end проверка на `PDFforTests/Консолидированная финансовая отчетность ПАО «Магнит» по МСФО за 1 полугодие 2025 год.pdf` показала, что спорный score `23.82 / critical` и ratios (`current_ratio=0.796`, `quick_ratio=0.345`, `financial_leverage=8.47`, `receivables_turnover=83.41`) математически совпадают и с backend payload, и с исходными строками PDF (`(в тысячах рублей)`, `Выручка 1 673 223 617`, `Прибыль за период ... 154 479`, `Итого обязательства 1 486 023 626`, `Итого капитал ... 175 381 814`). Реальной ошибкой оказался не parser, а frontend card `ScoreInsightsCard`: она жёстко называлась `AI-Инсайты и Аналитика` и показывала hardcoded AI-copy даже когда backend `nlp` пустой, а GigaChat фактически отвечает `402 Payment Required`/`429 Too Many Requests`. Карточка переведена в truthful mode: AI-заголовок показывается только при непустых `nlp.risks`/`nlp.key_factors`, иначе рендерится `Скоринговая аналитика` с явным сообщением, что AI-анализ недоступен и выводы построены по score-факторам. Параллельно стабилизирован flaky property-test `AnalysisHistory` (safe timestamp arbitrary вместо `fc.date().toISOString()`). Closure validation зелёная: `npm --prefix frontend run lint` → `OK`, `npm --prefix frontend run test` → `86 passed`.
- **Текущая агентская сессия**: 2026-04-01 — локальный non-Docker `503` на `POST /upload` расследован и снят без изменений продуктового кода. Корень оказался операционным: живой backend-процесс был привязан к `neofin_test` из-за эффекта `TESTING=1`, поэтому первый `INSERT INTO analyses` падал с `UndefinedTableError`, хотя `/system/health` продолжал возвращать `ok` (он проверяет только `SELECT 1`, а не наличие продуктовых таблиц). Backend перезапущен с явным process-level `TESTING=0` и `TASK_RUNTIME=background`; после этого `POST http://127.0.0.1:8000/upload` и `POST http://127.0.0.1:3000/api/upload` снова отдают `200` с `task_id`, а `/result/{task_id}` возвращает `status=processing`.
- **Текущая агентская сессия**: 2026-04-01 — non-Docker local runtime поднят успешно: backend запущен локально на `http://127.0.0.1:8000` с явным `TESTING=0` и `TASK_RUNTIME=background`, frontend Vite запущен на `http://127.0.0.1:3000`. Живые проверки прошли по обоим контурам: `GET /system/health` → `{"status":"ok","services":{"db":"ok","ai":"ok","ocr":"ok"}}`, `GET http://127.0.0.1:3000/` → `200`, `GET http://127.0.0.1:3000/api/system/health` через Vite proxy → `200`. Логи сохранены в `.tmp/backend.local.log` и `.tmp/frontend.local.log`; единственный замеченный startup-warning — `GIGACHAT_SSL_VERIFY=false`.
- **Текущая продуктовая сессия**: 2026-04-01 — выполнен `all-metrics extraction stabilization` pack: `pdf_extractor` теперь нормализует typographic quotes в text extraction path, подавляет `Consolidated Statements of ...` title noise, учитывает specificity keyword-match при выборе строк, жёстче фильтрует `liabilities` component-строки (lease/current/non-current) и разрешает hybrid `tables + text` extraction для balance section totals с обратным bridge `liabilities - long_term -> short_term`. Regression corpus расширен до support-метрик `ebitda/ebit/interest_expense/cost_of_goods_sold/average_inventory`, а `tests/test_pdf_regression_corpus.py` получил acceptance guard на покрытие всех 15 metric keys across `pdf_regression_corpus + real_fixtures + local demo manifest`. Closure validation зелёная: `tests/test_pdf_extractor.py` → `57 passed`, `tests/test_pdf_regression_corpus.py` → `9 passed`, `tests/test_pdf_real_fixtures.py` → `2 passed`, `RUN_LOCAL_PDF_REGRESSION=1 python -m pytest tests/test_pdf_local_magnit_regression.py -q` → `6 passed`, `tests/test_api.py tests/test_scoring.py tests/test_analysis_scoring.py` → `35 passed`.
- **Текущая продуктовая сессия**: 2026-03-31 — regression hotfix для свежего `MSFO/IFRS` patch закрыт: в `src/analysis/pdf_extractor.py` убраны опасные короткие IFRS/note-column коды (`3..17`) из table/text fallback, из-за которых примечания и номера колонок подменяли реальные totals на Magnit fixtures. Добавлены targeted regression tests против note-column collision, а closure validation снова зелёная: `tests/test_pdf_extractor.py` → `52 passed`, `RUN_LOCAL_PDF_REGRESSION=1 python -m pytest tests/test_pdf_local_magnit_regression.py -q` → `6 passed`, `tests/test_api.py tests/test_scoring.py tests/test_analysis_scoring.py tests/test_pdf_extractor.py` → `87 passed`.
- **Текущая продуктовая сессия**: 2026-03-31 — stale test cleanup после удаления legacy `analyze`-слоя выполнен: `tests/test_api.py` сужен до актуального `/upload` + `/result`, `tests/test_benchmarks.py` очищен от несуществующего controller import, а `tests/test_controllers_analyze.py` и `tests/test_controllers_analyze_coverage.py` удалены как obsolete surface. Closure validation по актуальному scorer/extractor-набору зелёная: `tests/test_api.py`, `tests/test_scoring.py`, `tests/test_analysis_scoring.py`, `tests/test_pdf_extractor.py` → `85 passed`. Одновременно зафиксирован важный regression signal после свежего pull: `RUN_LOCAL_PDF_REGRESSION=1 python -m pytest tests/test_pdf_local_magnit_regression.py -q` падает `6/6`; на real Magnit fixtures extractor теперь возвращает явно заниженные значения (`equity=7000`, `liabilities=5000`, `revenue=12024000`) вместо ожидаемых масштабов, то есть "быстрый" patch по МСФО не проходит локальный real-PDF guardrail.
- **Текущая агентская сессия**: 2026-03-31 — локальный репозиторий повторно синхронизирован с `origin/main` до `a5173bf` (`Update Python version in README`) через fast-forward; в pull вошли `4b7145c` (`Clean up & fixes`) и `ddd6eb6` (`Fix russian MSFO`). Targeted sanity-check `tests/test_pdf_extractor.py` зелёный (`50 passed`), но closure-check показал новую drift-проблему: после удаления `src/controllers/analyze.py` и `src/routers/analyze.py` в дереве остались старые тесты (`tests/test_api.py`, `tests/test_benchmarks.py`, `tests/test_controllers_analyze*.py`), которые падают на `ModuleNotFoundError` уже на collection phase.
- **Текущая агентская сессия**: 2026-03-31 — глобальная MCP-среда Codex дополнена ещё двумя рабочими контурами: подтверждено, что `Supabase MCP` уже подключён через `https://mcp.supabase.com/mcp` и не требует дублирования, а `Knowledge Graph Memory MCP` добавлен как `npx -y @modelcontextprotocol/server-memory` с постоянным файлом памяти `C:\Users\User\.codex\mcp-memory\knowledge-graph-memory.jsonl`. Глобальный `C:\Users\User\.codex\AGENTS.md` дополнен правилом использовать `Supabase MCP` для задач по Supabase и `Knowledge Graph Memory MCP` для долговременной памяти. Для подхвата новых MCP/skills в текущем desktop-сеансе по-прежнему желателен restart Codex.
- **Текущая агентская сессия**: 2026-03-31 — глобальный bootstrap Codex на этой машине синхронизирован под новую рабочую схему: установлен MCP `context7` через `codex mcp add context7 -- npx -y @upstash/context7-mcp`, в `C:\Users\User\.codex\AGENTS.md` добавлено правило использовать Context7 для library/API/setup запросов, а `obra/superpowers` клонирован в `C:\Users\User\.codex\superpowers` и подключён через native skill discovery junction `C:\Users\User\.agents\skills\superpowers`. Для активации discovery в текущем desktop-сеансе нужен restart Codex.
- **Текущая продуктовая сессия**: 2026-03-30 — подготовлен к публикации и проверен pending launch-pack, которого не хватало на GitHub для синхронизации локального состояния проекта: обновлены `pdf_extractor`, `scoring`, `settings`, `demo_manifest`, релевантные тесты и docs/env по `SCORING_PROFILE`. Таргет-валидация зелёная: `tests/test_scoring.py`, `tests/test_models_settings.py`, `tests/test_pdf_extractor.py` → `77 passed`.
- **Текущая продуктовая сессия**: 2026-03-30 — выполнен большой редизайн конкурсной презентации `NeoFin AI`: пакет `docs/contest_presentation_2026/` переведён с монолитного deck-script на модульную структуру `src/theme.js + src/renderers.js + src/content.js + entrypoint`, колода перестроена в формат `1–15 main story / 16–32 backup / 33 thanks`, визуальный стиль выровнен в `premium fintech`, а итоговая сборка `neo-fin-ai-molodoy-finansist-2026.pptx` проходит локально без overlap/out-of-bounds warnings. Zip-проверка подтверждает `33` слайда, `build-log.txt` чистый.
- **Текущая продуктовая сессия**: 2026-03-30 — выполнен `Magnit data-quality + retail scoring profile` pack: `pdf_extractor` получил metric-aware total/component фильтры (строгие правила для `current_assets`, `short_term_liabilities`, `accounts_receivable`), quality-aware tie-break и guardrail `current_assets < max(components) -> null + derive fallback`; в scanned P&L добавлен code-oriented pass (`2110/2400`) и sanity-защита от подмены `net_profit` через `совокупный финансовый результат периода`; в `scoring` добавлен `SCORING_PROFILE` (`generic`/`retail_demo`) через `BENCHMARKS_BY_PROFILE` без изменения API-контракта. Local regression для Magnit (`RUN_LOCAL_PDF_REGRESSION=1`) снова зелёный `6/6`, unit-срезы (`tests/test_pdf_extractor.py`, `tests/test_scoring.py`, `tests/test_models_settings.py`, `tests/test_analysis_scoring.py`) зелёные, frontend build/test зелёные.
- **Текущая продуктовая сессия**: 2026-03-30 — собрана конкурсная презентация `NeoFin AI` для «Молодой Финансист 2026»: создан отдельный пакет `docs/contest_presentation_2026/` с исходником на PptxGenJS (`neo-fin-ai-molodoy-finansist-2026.cjs`), локальными helper-модулями, `README.md` и готовым `neo-fin-ai-molodoy-finansist-2026.pptx`. Контент опирается на реальные продуктовые документы и demo-артефакты проекта, без спекулятивных claims про трейдинг/рынок; сборка проходит локально, а ограничение среды для визуальной проверки `.pptx` (нет `soffice` / PowerPoint COM / `python-pptx`) зафиксировано в `.agent/local_notes.md`.
- **Текущая продуктовая сессия**: 2026-03-30 — live smoke fallback-контура подтверждён повторно после runtime env-fix: при запуске backend/worker с `TESTING=0` и pre-step `python -m alembic upgrade head` все 3 demo-сценария (`text_single`, `scanned_single`, `multi_period_magnit`) проходят в `TASK_RUNTIME=celery`. Зафиксирован runbook-precondition: для demo/runtime path нельзя оставлять `TESTING=1` из `.env`.
- **Текущая продуктовая сессия**: 2026-03-30 — закрыт runtime/extraction блокер demo-smoke: `liabilities` в `scanned_single` больше не теряется из-за confidence-gate (`derived_strong` для balance-derived liabilities), OCR early-stop требует liability-side signal, а Celery worker переведён на persistent process-level event loop (убран `event loop is closed` на последовательных задачах). Fallback live-smoke (redis + локальные backend/worker) теперь последовательно проходит `text_single`, `scanned_single`, `multi_period_magnit`.
- **Текущая продуктовая сессия**: 2026-03-30 — выполнен operational smoke-run: полный путь `docker compose up -d --build` заблокирован внешним Docker pull/forwarding слоем (после восстановления daemon), fallback smoke (`redis compose + локальные backend/worker в TASK_RUNTIME=celery`) дал mixed результат: `text_single` ✅, `scanned_single` ❌ (`liabilities=None` против manifest), `multi_period_magnit` ✅. Для Windows-run дополнительно подтверждён риск `celery+asyncpg event loop is closed` на последовательных задачах.
- **Текущая продуктовая сессия**: 2026-03-30 — `final demo pack` (wave A-E) реализован: добавлен единый `tests/data/demo_manifest.json` (3 эталонных demo-сценария + headline ожидания), local regression `tests/test_pdf_local_magnit_regression.py` переведён на manifest-driven cases, добавлены repeatable smoke-скрипты (`scripts/demo_smoke.py`, `scripts/run_demo_smoke.ps1`, `scripts/run_demo_smoke.sh`), `DetailedReport.tsx` декомпозирован на report-компоненты с выносом constants/reliability helper и `crypto.randomUUID()` transaction id fallback, а docs drift закрыт через новый `docs/ROADMAP.md`, `docs/CONTEST_DEMO_RUNBOOK.md`, `docs/CONTEST_OPERATOR_CARD.md` и sync `README.md`.
- **Текущая продуктовая сессия**: 2026-03-30 — extraction reliability pack (deep-review) выполнен: `is_form_like` стал header-driven, `is_balance_like` отделён для section/guard paths, row-crop OCR лимиты обновлены до `max_attempts_per_spec=4` + page-budget `14`, derive для liabilities больше не режет valid high-leverage кейсы, а local Magnit regression теперь кэширует payload по `(filename, scanned)` и не повторяет OCR на одном scanned файле.
- **Текущая продуктовая сессия**: 2026-03-30 — `P5` из `docs/NEXT_SESSION_PLAN.md` выполнен: row-crop OCR для balance-строк теперь запускается только при layout-сигнале (`_should_run_layout_metric_row_crop`) и ограничен `max_attempts_per_spec=2` на кодовую строку, что снижает лишние OCR-crop операции на шумных страницах. Добавлены тесты на signal-gate, attempt cap и вызов row-crop только для релевантных страниц; regression-suite зелёный (`tests/test_pdf_extractor.py` `34/34`, `RUN_LOCAL_PDF_REGRESSION=1` `6/6`).
- **Текущая продуктовая сессия**: 2026-03-30 — `P4` из `docs/NEXT_SESSION_PLAN.md` выполнен: local scanned regression расширен до трёх scanned-кейсов (один полный + два инвариантных среза на том же real scanned Magnit), отдельно зафиксированы `inventory`, `accounts_receivable`, `short_term_liabilities` и bridge `long_term = liabilities - short_term`. Локальный harness: `tests/test_pdf_local_magnit_regression.py` теперь `6/6`.
- **Текущая продуктовая сессия**: 2026-03-30 — `P3` из `docs/NEXT_SESSION_PLAN.md` выполнен: post-parse проверки form-like OCR вынесены в единый `_apply_form_like_guardrails(...)` блок с инвариантами `component <= subtotal <= total` и soft-null для подозрительных subtotal/total значений. Добавлены regression-тесты для кейсов `liabilities > total_assets` и `short_term_liabilities > total_assets`; валидация зелёная (`tests/test_pdf_extractor.py` `31/31`, `RUN_LOCAL_PDF_REGRESSION=1` `4/4`).
- **Текущая продуктовая сессия**: 2026-03-30 — `P2` из `docs/NEXT_SESSION_PLAN.md` выполнен: код `1400` перестал трактоваться как `liabilities` и теперь используется как internal `long_term_liabilities` для безопасного derive `liabilities = long_term + short_term` с cross-check через `assets - equity`. Добавлены guards против завышения обязательств на конфликтном OCR section IV и regression-check на real scanned Magnit (`long_term = liabilities - short_term = 33 723 849 000`). Верификация: `tests/test_pdf_extractor.py` `29/29`, `RUN_LOCAL_PDF_REGRESSION=1` `4/4`.
- **Текущая продуктовая сессия**: 2026-03-30 — `P1` из `docs/NEXT_SESSION_PLAN.md` выполнен: layout-aware row-crop OCR extraction обобщён на balance-коды `1200/1210/1230/1250/1400/1500` через единый spec-driven pass. Добавлены unit-regressions на multi-code synthesis и фильтрацию короткого section-noise; после фикса ложного `short_term_liabilities=8 609 000` real-fixture suite снова зелёный (`tests/test_pdf_extractor.py` `26/26`, `RUN_LOCAL_PDF_REGRESSION=1` `4/4`).
- **Текущая продуктовая сессия**: 2026-03-29 — `inventory` extraction на scanned Magnit доведён: в `extract_text_from_scanned()` добавлен layout-aware row-crop OCR pass для строки `Запасы` (код `1210`) с подавлением trailing single-digit шума, из которого синтезируется безопасная строка `Запасы <значение>` для текущего parser flow. На `magnit_2025_q1_scanned` теперь: `inventory=2142153000.0`, `accounts_receivable=26998240000.0`; local regression `RUN_LOCAL_PDF_REGRESSION=1` проходит `4/4`, а `tests/test_pdf_extractor.py` — `24/24`.
- **Текущая продуктовая сессия**: 2026-03-29 — `inventory/accounts_receivable` scanned OCR pass завершён: `_TEXT_LINE_CODE_MAP` расширен кодами `1210/1230`, form-like multiline extraction получил same-line OCR parsing и фильтр строк с чужими metric keywords, что вернуло `accounts_receivable=26998240000.0` для `magnit_2025_q1_scanned` при сохранении safe `inventory=None`; closure `local code_review` pass выполнен (блокирующих замечаний нет), локальная верификация: `tests/test_pdf_extractor.py` (`23/23`) и `RUN_LOCAL_PDF_REGRESSION=1` (`4/4`).
- **Текущая продуктовая сессия**: 2026-03-29 — local Magnit regression sync завершён: для `magnit_2025_q1_scanned` в `tests/test_pdf_local_magnit_regression.py` ожидание `short_term_liabilities` зафиксировано как `192460146000.0`, полный локальный набор (`RUN_LOCAL_PDF_REGRESSION=1`) проходит `4/4`.
- **Текущая продуктовая сессия**: 2026-03-29 — `short_term_liabilities` extraction pack завершён: form-like OCR path теперь отдельно извлекает `Итого по разделу V/У` и `Итого краткосрочных обязательств`, text line-code fallback расширен на код `1500`, а sanity-check в scanned ветке не допускает значения `short_term_liabilities > liabilities`.
- **Текущая продуктовая сессия**: 2026-03-29 — public docs runtime sync выполнен: `README.md` теперь явно включает `Redis` и `Celery` в публичный стек и в описании runtime-контура, выровнен по смыслу с `docs/ARCHITECTURE.md` и `docs/CONFIGURATION.md`.
- **Текущая продуктовая сессия**: 2026-03-29 — `Magnit scanned/OCR quality pass` добит: post-page OCR останавливается рано только после появления достаточного сигнала русской бухгалтерской формы, OCR-specific numeric parser корректно берёт `1348 503` как одно значение и аккуратно режет склеенные multi-period runs, а layout-aware section totals (`Итого по разделу`) возвращают `equity` и позволяют безопасно вывести `liabilities = assets - equity`. На scanned Магните Q1 2025 сейчас: `revenue=103015000`, `net_profit=1348503000`, `cash_and_equivalents=1448897000`, `total_assets=435659511000`, `current_assets=174989150000`, `equity=209475516000`, `liabilities=226183995000`, `ocr_s≈11s`.
- **Фаза**: Phase 1 (MVP) — neofin-competition-release завершён; фича llm-financial-extraction реализована полностью
- **Последний зафиксированный коммит до текущей волны**: `docs(agent): harden invocation protocol and synthesis`
- **Текущая продуктовая сессия**: 2026-03-29 — `Magnit real-PDF accuracy + performance pass` выполнен: `extract_tables()` теперь идёт по `stream -> lattice fallback`, при переполнении таблиц оставляет top-10 по финансовой релевантности, а не по длине, `_detect_scale_factor()` распознаёт русские statement headers Магнита, line/text numeric selection пропускает номера примечаний `23/24` перед реальными суммами, а optional local regression harness под `PDFforTests` фиксирует реальные значения `revenue/net_profit/total_assets/liabilities` для трёх PDF Магнита. На digitized отчётах Магнита latency table-path упала примерно с `~80–88s` до `~23–30s`, а `revenue/net_profit` теперь выходят корректно.
- **Текущая продуктовая сессия**: 2026-03-29 — focused extraction quality pack завершён: `pdf_extractor` теперь корректно разбирает англоязычные grouped numbers с запятыми и отрицательные значения в скобках, ищет scale marker только рядом с реальными statement headers, предпочитает `text_regex` над `table_partial`, а line-based text selection ловит `revenues / net income / total stockholders' equity / total current assets / total current liabilities` на real annual reports. Manifest-driven real-fixture smoke pack синхронизирован под реальные значения CorVel/Cloudflare, а scoring получил guardrail против слишком оптимистичного итогового score на бедном наборе критичных метрик.
- **Текущая продуктовая сессия**: 2026-03-29 — `persistent runtime wave 2A` реализована: cancellation truth source перенесён в БД, `analyses` и `multi_analysis_sessions` получили `cancel_requested_at/cancelled_at/runtime_heartbeat_at`, одиночный и многопериодный runtime перешли на cooperative cancellation, а API/TS contracts теперь знают `cancelling` и `cancelled`.
- **Текущая продуктовая сессия**: 2026-03-29 — `persistent runtime wave 2B` реализована: появились timeout-настройки heartbeat recovery, bounded stale recovery job (`scripts/runtime_recover.py`) и CRUD-path для перевода зависших runtime-строк в `failed` с `reason_code=runtime_stale_timeout`, без auto-requeue.
- **Последняя сессия**: 2026-03-28 — после DB schema evolution и bounded admin cleanup job зафиксирован новый execution plan: `cleanup operationalization` → `test hygiene` → `optional heavy/OCR real-PDF tier` → `persistent runtime` → `production hardening`.
- **Последняя продуктовая сессия**: 2026-03-28 — `test hygiene` wave 2 завершена: stale `multi_analysis` router tests выровнены под текущий multipart/status contract, `TestClient` lifecycle cleanup внесён в router/websocket/system tests, а `POST /multi-analysis` при невалидной метке периода теперь возвращает корректный `422` вместо `500` и удаляет временный файл при ошибке валидации.
- **Текущая продуктовая сессия**: 2026-03-29 — первая версия `persistent runtime` внедрена: `TASK_RUNTIME=background|celery`, Celery + Redis dispatch layer, worker-friendly entrypoints поверх `process_pdf/process_multi_analysis`, Redis event bridge для WebSocket-статусов, router-level `TaskRuntimeError → 503`, compose/env/docs/test coverage синхронизированы.
- **Текущая продуктовая сессия**: 2026-03-29 — Redis runtime подтверждён вживую: compose переведён на `redis:8.6-alpine`, минимальный локальный compose очищен от obsolete `version`, контейнер `redis-test` отвечает `PONG`, а локальный Celery worker из новой `env` успешно подключается к `redis://127.0.0.1:6379/0` и выходит в `ready`.
- **Текущая продуктовая сессия**: 2026-03-29 — локальный `docker-compose.yml` приведён обратно к полноценному dev-стеку: `backend + worker + backend-migrate + frontend + db + redis`, `db_test` и `ollama` переведены в profiles, а для runtime-сервисов принудительно выставлен `TESTING=0`, чтобы они не уезжали в `TEST_DATABASE_URL` из локального `.env`.
- **Текущая продуктовая сессия**: 2026-03-29 — публичные docs синхронизированы с новым runtime-контуром: `docs/ARCHITECTURE.md`, `docs/CONFIGURATION.md` и `README.md` теперь согласованы по Redis/worker, production Docker-path, HuggingFace-провайдеру и текущему статусу HTTPS.
- **Текущая продуктовая сессия**: 2026-03-29 — bootstrap новой проектной `env` снова рабочий: корневая причина падения `pip install -r requirements.txt` оказалась в несуществующей версии `pdfplumber~=0.12.0`; зависимость возвращена к опубликованной `pdfplumber~=0.11.9`, полный install path в новой `env` проходит.
- **Текущая продуктовая сессия**: 2026-03-29 — post-review polishing по `pdfplumber` завершён: комментарий в `requirements.txt` теперь честно фиксирует, что `0.11.9` — latest published PyPI release на момент фикса, а stale regression test больше не требует несуществующий `0.12.x`.
- **Текущая продуктовая сессия**: 2026-03-29 — финальный polishing-pass по heavy real-PDF tier: ARC4 suppression снова локализован в конкретные PDF-related тестовые модули вместо глобального `conftest.py`, `.gitignore` получил явный комментарий к negate-rule, а `tests/data/pdf_real_fixtures/README.md` теперь документирует и `RUN_PDF_REAL_HEAVY`, и `--run-pdf-real-heavy`, и ожидаемый CI/nightly usage.
- **Текущая продуктовая сессия**: 2026-03-28 — `optional heavy/OCR real-PDF tier` добавлен как отдельный gated regression layer: `tests/test_pdf_real_heavy_fixtures.py` и `manifest_heavy.json` проверяют full pipeline на committed annual reports, но запускаются только при `RUN_PDF_REAL_HEAVY=1`, не замедляя default green path.
- **Текущая агентская сессия**: 2026-03-28 — локальные orchestration rules усилены обязательным `code_review` closure gate: после каждого пакета уровня `cross-module` и выше нужен явный review-этап, даже если внешний role-native запуск `code_review` не выполняется.
- **Текущий агентский технический долг**: 2026-03-29 — в NeoFin всё ещё нет честного внешнего `role-native` runtime; manifests и launch protocol уже есть, но enforceable external role launch по-прежнему отсутствует, поэтому безопасный default остаётся `local role-guided synthesis`.
- **Последняя сессия по публичной документации**: 2026-03-28 — GitHub-facing docs дополнительно очищены от внутренних ссылок и англоязычного шума; после расширения `docs/BUSINESS_MODEL.md` синхронизированы `README.md` и `docs/ARCHITECTURE.md`: публичная витрина, экономическое обоснование и техническая архитектура теперь говорят одной логикой о профессиональном контуре, объяснимости и практической состоятельности продукта.
- **Последнее обновление документации**: 2026-03-28 — из `AGENTS.md` вынесены операционные блоки в `.agent/architecture.md`, `.agent/checklists.md`, `.agent/modes.md`
- **Контекст**: Полная архитектура в `.agent/architecture.md` и `docs/ARCHITECTURE.md`. Читать перед любой разработкой.

---

✅ **Autopilot migration complete** — reusable Autopilot foundation, runtime diagnostics, machine-readable model selection и roadmap ownership перенесены в отдельный репозиторий `E:\codex-autopilot`.
✅ **NeoFin cleanup** — основной репозиторий больше не хранит experimental Autopilot spike как активный рабочий слой; здесь остаются только продуктовый код, human-readable agent workflow и исторические записи в `PROJECT_LOG.md`.

✅ **Qwen Regression Fixes 2** — исправлены все 10 багов: `_normalize_number` (Unicode-минус), `_extract_first_numeric_cell` (4-значные ячейки), `analyze_narrative` (пустой текст в LLM), `extract_text_from_scanned` (MAX_OCR_PAGES=50), `translate_ratios` (утечка ключей), `_format_metric_value` (отрицательные числа), `_parse_recommendations_response` (дедупликация + f-строки), `generate_recommendations` (timeout=90), `_log_missing_data` (f-строки). 20/20 тестов зелёные.

✅ **Hook System** — 8 хуков: architecture-guard, agents-rules-reminder, python-lint-on-save (flake8 per-file), review-refactor-verify, run-tests-after-task, update-session-docs, generate-commit-message, check-local-notes-before-task

✅ **Scale factor в parse_financial_statements_with_metadata** — `_detect_scale_factor` теперь вызывается внутри функции, scale применяется к монетарным метрикам при сборке результата. Корень ROA=2290329% устранён.
✅ **Scoring аномалии** — `_ANOMALY_LIMITS` в `scoring.py` блокирует аномальные коэффициенты от нормализации в "идеал".
✅ **NLP токены** — `nlp_analysis.py` использует `is_clean_financial_text` + `clean_for_llm` перед LLM.
✅ **Unicode minus** — `_normalize_number` обрабатывает U+2212 и trailing minus.
✅ **Приоритизация источников** — `_raw_set()` заменяет прямые записи в `raw`, table_exact побеждает text_regex.
✅ **OOM при OCR** — постраничная обработка в `extract_text_from_scanned`.
✅ **Audit Wave 1** — закрыты подтверждённые product bugs:
  - frontend `interfaces.ts` синхронизирован с `critical` risk level
  - polling fallback в `AnalysisContext.tsx` снова читает `data` из `/result/{task_id}`
  - `DetailedReport.tsx` и `AnalysisHistory.tsx` корректно отображают `critical`
  - `/analyses/{task_id}` теперь возвращает inner `data`, а не вложенный root payload
  - `tasks.py` снова шлёт промежуточные WebSocket статусы `extracting/scoring/analyzing`
  - `process_multi_analysis()` больше не отдаёт неподдерживаемый статус `completed_with_errors`
  - `pdf_extractor.py` больше не отбрасывает table-extracted малые monetary values и поддерживает mock-friendly OCR fallback
  - `Dockerfile.backend` копирует `entrypoint.sh`, что снимает риск падения `backend-migrate`
✅ **Audit Wave 2** — production Docker hardening:
  - `docker-compose.prod.yml` теперь собирает `nginx` напрямую из `frontend/Dockerfile.frontend`
  - production deploy больше не зависит от локального bind mount `frontend/dist`
  - `frontend/nginx.prod.conf` получил rate limiting, CSP, proxy error page и более жёсткий proxy path
  - `scripts/deploy-prod.sh` переведён на `docker compose` и добавляет `config` validation перед build
  - docs и agent-checklists синхронизированы с новым deploy path
✅ **Audit Wave 3** — LLM token optimization and test hardening:
  - `src/analysis/llm_extractor.py` теперь выкидывает year/page noise, дедуплицирует и ранжирует строки по финансовому сигналу
  - `extract_with_llm()` больше не отказывается от длинного отчёта сразу: вход сначала compact-ится до token budget
  - `chunk_text()` корректно режет монолитные oversized paragraphs
  - `src/analysis/nlp_analysis.py` формирует компактный narrative excerpt до вызова `ai_service`
  - `src/analysis/recommendations.py` переведён на compact JSON prompt context
  - `tests/test_nlp_analysis.py` и связанные LLM tests синхронизированы с актуальным `ai_service` contract
  - `src/analysis/pdf_extractor.py` теперь использует newline-safe keyword-window extraction для section totals и nearby numbers
  - OCR TypeError fallback больше не может молча обойти `MAX_OCR_PAGES`
  - `tests/test_pdf_extractor.py` покрывает anti-merge и fallback page-cap regressions
  - `src/analysis/pdf_extractor.py` пропускает year markers в `_extract_first_numeric_cell()`, чтобы multi-period rows не превращали `2023` в extracted metric
  - `tests/data/pdf_regression_corpus.json` и `tests/test_pdf_regression_corpus.py` фиксируют corpus pack для сложных table layouts
✅ **Audit Wave 3B — DB hardening**:
  - `src/db/database.py` теперь реально применяет `DB_POOL_TIMEOUT` / `DB_POOL_RECYCLE` и предпочитает `TEST_DATABASE_URL` при `TESTING=1`
  - `src/app.py` вызывает `dispose_engine()` на shutdown, чтобы не оставлять висящие pooled connections
  - `src/db/models.py` + миграция `0004_harden_db_status_constraints.py` добавляют status constraints и lifecycle index `(status, updated_at)` для `multi_analysis_sessions`
  - `src/db/crud.py` больше не маскирует read failures БД под `None`
  - router boundary (`analyses`, `pdf_tasks`, `multi_analysis`) переводит SQLAlchemy failures в явный `DatabaseError`
  - `src/utils/error_handler.py` регистрирует catch-all handler последним, чтобы специализированные DB handlers не деградировали в generic 500
✅ **Audit Wave 3C — DB schema evolution**:
  - `src/db/models.py` и миграция `0005_add_analysis_summary_columns.py` добавляют typed summary columns для `analyses`: `filename`, `score`, `risk_level`, `scanned`, `confidence_score`, `completed_at`, `error_message`
  - `src/db/crud.py` держит dual-write invariant: summary-поля выводятся из того же JSONB snapshot, а status-only update не стирает уже сохранённый payload
  - `src/routers/analyses.py` предпочитает typed summary columns для `/analyses`, но остаётся совместимым с legacy rows через fallback на `result`
  - добавлены maintenance helpers для bounded cleanup analyses / multi-analysis sessions с `dry_run=True`
  - API shape не менялся: `result` остаётся каноническим источником для detail/history compatibility
✅ **Audit Wave 3D — Scheduled/Admin Cleanup Job**:
  - `src/maintenance/cleanup_jobs.py` добавляет bounded orchestration для cleanup job без смешивания maintenance-логики с `tasks.py`
  - `src/maintenance/admin_cleanup.py` и `scripts/admin_cleanup.py` дают отдельный admin/cron entrypoint с `dry_run` по умолчанию
  - `src/db/crud.py` re-check'ит status/age прямо в `DELETE`, уменьшая race window между candidate selection и delete
  - v1 policy чистит только stale `uploading/processing` analyses и stale `processing` multi-sessions
  - completed business history не удаляется scheduled path по умолчанию
✅ **Agent Workflow Hardening**:
  - `AGENTS.md` и `.agent/subagents/README.md` переведены на lean orchestration policy вместо fan-out “запускать всех”
  - стартовый default bundle ужат до `0 или 1` внешнего субагента; второй pass подключается только по явному риску
  - `test_planner`, `code_review`, `docs_keeper` переведены в phase-based late-stage agents
  - добавлены `.agent/subagents/*.toml` manifests с preferred model, prompt, auto-triggers и anti-triggers
  - система разделена на core orchestration, product-domain specialists и rare governance/release guards
✅ **Agent Workflow Hardening 2**:
  - `orchestration mode` больше не трактуется как обязательный внешний вызов субагента; `0 external subagents` допустимы, если independent pass не даёт новой информации
  - введён invocation budget: `core-auto`, `domain-auto`, `phase-gated`, `manual-explicit`
  - `policy_guardian`, `compliance_guardian`, `api_versioning_guardian`, `audit_guardian`, `backup_guardian`, `feature_flag_guardian` закреплены как non-default/manual-explicit guards
  - `devops_release`, `runtime_guardian`, `deployment_guardian`, `error_monitoring_guardian`, `usability_guardian` закреплены как phase-gated, а не стартовые агенты
✅ **Agent Workflow Hardening 3**:
  - `contracts_guardian` повышен до `gpt-5.4` / `high` как core contract-risk guard для финансового продукта
  - `performance_guardian` теперь явно работает и до, и после реализации: без diff даёт гипотезы, с diff — фактический hotspot review
  - граница `devops_release` vs `deployment_guardian` усилена явными anti-trigger пояснениями в самих manifest prompts
  - в `AGENTS.md`, `.agent/subagents/README.md` и root `README.md` явно зафиксировано различие между `.toml` registry entries и `.md`-only product role-specs
✅ **Agent Workflow Hardening 4**:
  - жёстко разведены `project-role` и `runtime carrier`
  - `explorer` / `default` / `worker` теперь прямо зафиксированы как tool-level carriers, а не замена registry-role
  - добавлен прямой запрет на паттерн “generic explorer с prompt ‘действуй как solution_designer’”
  - orchestration report теперь должен явно называть и роль субагента, и carrier исполнения
✅ **Agent Workflow Hardening 5**:
  - введён hard invocation protocol: внешний вызов считается валидным только при явном role-binding
  - добавлен deep synthesis ladder для very-complex cross-layer задач вместо хаотичного fan-out
  - добавлен failure diagnostics path для invalid/contradictory/timeouted orchestration passes
  - adaptive review loop зафиксирован как manual documentation hardening, без скрытого runtime-слоя
✅ **Agent Workflow Hardening 6**:
  - зарегистрирован `public_docs_guardian` как отдельный phase-gated guard для публичной витрины проекта
  - новая роль проверяет только `README.md` и GitHub-facing `docs/*`, не трогая `AGENTS.md` и `.agent/*`
  - мандат роли: хороший русский язык, деловой и презентационный тон, синхронность публичных документов, отсутствие лишних англицизмов
  - граница с `docs_keeper` явно разведена: `docs_keeper` отвечает за общий docs drift и closure ritual, `public_docs_guardian` — только за public-facing docs
  - роль не должна auto-invoke'иться на internal-doc задачах и не входит в стартовый bundle
✅ **Agent Workflow Hardening 7**:
  - введён локальный `Role-Native Launch Protocol` для NeoFin без возврата runtime foundation
  - добавлен обязательный `Invocation Record Template` перед любым внешним вызовом
  - теперь жёстко разведены три режима:
    - `role-native delegation`
    - `local role-guided synthesis`
    - запрещённый `carrier-only invocation`
  - введены `Launch Gate` и `Abort Gate`: если role-native identity нельзя доказать отдельно от carrier, внешний вызов отменяется
  - безопасный default для NeoFin зафиксирован как `local role-guided synthesis`, пока в проекте нет отдельного role-native runtime
⚠️ **Технический долг orchestration**:
  - protocol/spec layer уже есть, но настоящего внешнего role-native runtime всё ещё нет
  - это значит, что external delegation остаётся ограниченной и не может считаться полностью runtime-enforced
  - правильное место для полноценного решения — `E:\codex-autopilot`, а не продуктовый код NeoFin
✅ **Execution Plan Refresh**:
  - `docs/ROADMAP.md` переведён из общего wishlist в актуальный execution plan
  - ближайший порядок работ зафиксирован так:
    - cleanup job operationalization
    - test hygiene / warning cleanup
    - optional heavy/OCR real-PDF tier
    - persistent runtime вместо in-process `BackgroundTasks`
    - production hardening (VPS smoke, HTTPS, backup/restore)
✅ **Test Hygiene / Warning Cleanup (wave 2)**:
  - `tests/test_multi_analysis_router.py` переведён на hermetic multipart-post path с явным мокированием `process_multi_analysis`
  - stale completed-session fixtures дополнены `extraction_metadata` по текущему schema contract
  - property-based `multi_analysis` tests больше не ломают pytest fixture resolution
  - `tests/test_analyses_router.py`, `tests/test_routers_system.py` и `tests/test_websocket_integration.py` закрывают `TestClient` через context manager
  - `src/routers/multi_analysis.py` теперь переводит ошибки `PeriodInput` в корректный `422` и удаляет временный файл при validation failure
✅ **Test Hygiene / Warning Cleanup (wave 3)**:
  - `src/routers/multi_analysis.py` получил общий cleanup всех `temp_paths` до успешного handoff в `BackgroundTasks`
  - добавлены regression tests на cleanup temp PDF при validation failure и при `create_multi_session()` DB failure
  - глобальный ARC4 suppression убран из `pytest.ini`; локальная фильтрация перенесена в модули, которые реально импортируют PDF stack
  - `tests/test_multi_analysis_router.py` больше не использует permissive `TestClient` как общую фикстуру
  - `tests/test_tasks.py` использует helper `_assert_completed_status_called(...)` вместо fragile `call_args_list[-1][0][1]`
  - root `README.md` синхронизирован с текущей повесткой, чтобы roadmap был виден не только в agent docs
✅ **Error Contract Hardening**:
  - `src/app.py` регистрирует exception handlers при инициализации приложения, поэтому `DatabaseError` и другие app-level исключения реально мапятся в HTTP-ответы на router path
  - `src/routers/multi_analysis.py` логирует полные `ValidationError` details, но клиенту возвращает стабильный `422` с санитизированным сообщением
  - `tests/test_multi_analysis_router.py` фиксирует канонический DB-error ответ `503` с `DATABASE_ERROR` payload и сохраняет cleanup temp PDF на DB failure
✅ **Heavy Real-PDF Tier Hardening**:
  - `tests/test_pdf_real_heavy_fixtures.py` больше не читает `manifest_heavy.json` на import/collection path
  - heavy-tier теперь включается через `RUN_PDF_REAL_HEAVY=1` или `--run-pdf-real-heavy`
  - при отключённом heavy-tier default pytest path получает clean skip вместо collection failure
  - при missing/invalid manifest heavy-tier отдаёт понятный skip reason с путём к manifest
  - ARC4 suppression для PDF stack централизован в `tests/conftest.py`; дубли из отдельных тестовых модулей убраны
✅ **Public Docs Cleanup**:
  - `README.md`, `docs/API.md`, `docs/ARCHITECTURE.md`, `docs/CONFIGURATION.md` дополнительно переведены на более чистый русский язык без лишнего внутреннего/инженерного тона
  - из публичной документации убраны ссылки на внутренние agent-docs и roadmap surfaces
  - `docs/BUSINESS_MODEL.md` больше не выглядит как агрессивный инвестиционный меморандум: документ переведён в профессиональный финансовый язык, с сегментацией B2B/B2C, приоритетом B2B, приоритетными стартовыми нишами, рабочей тарифной логикой, контрольными ориентирами удельной экономики, каналами продаж, риск-картой и сценарным прогнозом
  - дополнительным проходом убраны “Глава 3” и учебная нумерация разделов; финальная версия звучит как сильное публичное обоснование проекта, а не как черновик главы диплома
  - следующим проходом `docs/BUSINESS_MODEL.md` дополнительно расширен до конкурсного варианта: появились разделы `Почему проект актуален именно сейчас`, `Портрет приоритетного клиента`, `Конкурентная среда и позиционирование`, `Продукт как сервис и как корпоративный инструмент`, `Логика принятия решения о покупке`, `Структура выручки по сегментам`, `Операционный рычаг модели`, `Ключевые показатели управленческого контроля`, `Условия достижения базового сценария`
  - после этого `README.md` синхронизирован с бизнес-моделью: добавлены блоки `Для кого предназначен проект`, `Практическая ценность`, `Коммерчески значимые свойства продукта`, чтобы публичная подача на GitHub и расширенный документ звучали как единый пакет
  - финальным polishing-pass дополнительно синхронизирован `docs/ARCHITECTURE.md`: архитектура теперь лучше объясняет, почему продуктовая ценность NeoFin AI опирается на воспроизводимость, объяснимость, двухконтурное развёртывание и профессиональный сценарий использования
  - косметический финальный проход по публичным docs дополнительно вычистил смешанный язык и англицизмы в `README.md`, `docs/API.md`, `docs/ARCHITECTURE.md`, `docs/CONFIGURATION.md` и слегка полировал формулировки в `docs/BUSINESS_MODEL.md`, чтобы пакет выглядел цельно и профессионально на GitHub
✅ **Test Hygiene / Warning Cleanup (wave 1)**:
  - `tests/test_tasks.py` снова hermetic: success-path тесты `process_pdf` замокали `src.tasks.get_analysis` и больше не выходят в реальную БД
  - устранён fallout warning `coroutine 'Connection._cancel' was never awaited`, который появлялся после accidental asyncpg path
  - в `pytest.ini` явно задан `asyncio_default_fixture_loop_scope = function`
  - транзитивный `pypdf` ARC4 deprecation warning подавлен targeted filter'ом в pytest config
  - targeted suite `tests/test_tasks.py tests/test_api.py tests/test_app_coverage.py` снова проходит чисто: `36 passed`

## Что работает
✅ **POST /upload** — валидация PDF (magic header, ≤50MB), SpooledTemporaryFile, BackgroundTask, немедленный ответ с `task_id`
✅ **WebSocket Updates** — real-time уведомления о статусе задач через `/ws/{id}`; внедрён `ConnectionManager` (Singleton)
✅ **PDF extraction** — PyPDF2 (текст), camelot/pdfplumber (таблицы), pytesseract (OCR для сканов); улучшена детекция сканов через проверку `/Image` объектов
✅ **OCR Regression Guard** — multiline-safe numeric helpers и page-cap enforcement в fallback path защищают от silent giant-number regressions
✅ **Complex Table Layout Guard** — corpus pack защищает note/year columns, RSBU line-code layouts, garbled labels и OCR pseudo-tables
✅ **Real-PDF Smoke Guard** — `tests/data/pdf_real_fixtures/` и `tests/test_pdf_real_fixtures.py` проверяют committed annual-report fixtures по manifest-driven subset assertions и file provenance
✅ **Real-PDF Heavy Guard** — `tests/test_pdf_real_heavy_fixtures.py` и `manifest_heavy.json` дают optional heavy-tier для full pipeline на реальных annual reports; запуск только по явному флагу `RUN_PDF_REAL_HEAVY=1`
✅ **Financial ratios** — 13 коэффициентов (4 группы: ликвидность, рентабельность, устойчивость, активность); RU-ключи → EN через `translate_ratios()`
✅ **Integral scoring** — скоринг 0–100, risk_level (пороги 75/55/35, уровни: low/medium/high/critical), factors, normalized_scores; добавлено поле `confidence_score` для оценки полноты данных
✅ **Scoring factors** — осмысленные описания факторов с ссылками на бенчмарки (вместо просто "Значение: 1.23")
✅ **4-уровневая система риска** — low (≥75) / medium (55–74) / high (35–54) / critical (<35) для более гранулярной оценки
✅ **NLP analysis** — риски и ключевые факторы через `ai_service.py` (GigaChat → DeepSeek → Ollama → graceful degrade)
✅ **AI Agents Refactoring** — внедрён `BaseAIAgent`, исправлена утечка ресурсов в GigaChat (Singleton ClientSession), внедрены экспоненциальные ретраи
✅ **Recommendations** — `src/analysis/recommendations.py`: 3–5 рекомендаций с явными ссылками на метрики; outer timeout 90s; fallback при недоступности AI; подключено в `tasks.py`
✅ **LLM Prompt Budgeting** — extractor/NLP/recommendations используют deterministic prompt compaction: dedupe, ranking по signal, trimming до budget без изменения внешнего API
✅ **GET /analyses** — список анализов с пагинацией (page, page_size ≤ 100), сортировка по created_at DESC, auth X-API-Key
✅ **Masking** — `src/utils/masking.py`: чистая функция `mask_analysis_data(data, demo_mode)`, применяется во всех трёх эндпоинтах при `DEMO_MODE=1`
✅ **AnalysisHistory.tsx** — подключена к реальному API (`GET /analyses`), пагинация Mantine, skeleton/error states
✅ **DetailedReport.tsx** — BarChart из реальных `result.ratios`, цветовое кодирование по порогам, WebSocket-синхронизация
✅ **БД** — PostgreSQL 16, SQLAlchemy async, 5 миграций Alembic; hybrid storage для `analyses` (`result` JSONB + typed summary columns для history/cleanup) и bounded admin cleanup path для stale in-progress rows
✅ **Auth.tsx** — pre-flight `GET /api/analyses` с введённым ключом
✅ **CI/CD** — GitHub Actions: lint → test → security → build
✅ **Docker** — backend, frontend/nginx, db, db_test, ollama
✅ **Regex fallback** — извлечение 15 метрик через regex patterns (перенесено в `analysis` слой), если camelot не извлёк таблицы
✅ **Тесты** — подтверждённые локальные прогоны после audit wave 1:
  - `tests/test_scoring.py`, `tests/test_pdf_extractor.py`, `tests/test_api.py` → 18 passed
  - `tests/test_analyses_router.py`, `tests/test_tasks.py` → 27 passed
  - `tests/test_llm_extractor.py`, `tests/test_llm_extractor_properties.py`, `tests/test_nlp_analysis.py`, `tests/test_nlp_analysis_coverage.py`, `tests/test_recommendations.py` → 128 passed
  - после PDF hardening: `tests/test_pdf_extractor.py` → 8 passed; `tests/test_scoring.py tests/test_pdf_extractor.py tests/test_api.py` → 21 passed
  - после corpus/regression dataset: `tests/test_pdf_regression_corpus.py` → 7 passed; `tests/test_pdf_extractor.py tests/test_pdf_regression_corpus.py tests/test_scoring.py tests/test_api.py` → 28 passed
  - после DB hardening: `tests/test_api.py tests/test_analyses_router.py tests/test_routers_pdf_tasks.py tests/test_multi_analysis_db_errors.py tests/test_db_database.py tests/test_db_crud.py tests/test_app_coverage.py -q` → 75 passed
  - legacy `tests/test_tasks_coverage.py` остаётся устаревшим compatibility-suite и не отражает текущий product contract
✅ **Production Docker** — `Dockerfile.backend` (multi-stage), `Dockerfile.frontend` (multi-stage), `docker-compose.prod.yml`, `nginx.conf`, `scripts/deploy-prod.sh`
✅ **Production Frontend Image** — compose собирает frontend/Nginx образ сам, без внешнего `frontend/dist`
✅ **Persistent Task Runtime (v1)**:
  - `src/core/task_queue.py` добавляет конфигурируемый dispatch layer: `background` для локального пути и `celery` для Redis-backed worker
  - `src/core/runtime_events.py` публикует task/session status events через Redis pub/sub и поднимает bridge в FastAPI lifespan
  - `src/routers/pdf_tasks.py` и `src/routers/multi_analysis.py` больше не привязаны только к in-process `BackgroundTasks`; dispatch failures переводятся в `TaskRuntimeError` и canonical `503`
  - `src/tasks.py` продолжает быть каноническим pipeline entrypoint и использует `broadcast_task_event(...)` вместо прямой привязки к локальному `ws_manager`
  - `docker-compose.yml` и `docker-compose.prod.yml` теперь описывают `redis` + `worker` рядом с `backend`
✅ **Code Quality** — полная чистка неиспользуемых импортов, исправление линтера, переход на Pydantic-settings для управления env

---


## Исправления сессии 2026-03-27 (Большой коммит: scoring + LLM extraction + WebSocket + cleanup)
✅ **Коммит `b8ffaef`** — feat(scoring): enhance business model with contextual descriptions and 4-level risk system
✅ **Scoring Model Refinement** — 4-уровневая система риска (low/medium/high/critical), осмысленные описания факторов с ссылками на бенчмарки
✅ **LLM Financial Extraction** — полная реализация llm_extractor.py с chunking, anomaly detection, fallback merging
✅ **WebSocket Integration** — ws_manager.py (ConnectionManager), useAnalysisSocket.ts hook, real-time task updates
✅ **AI Agents Refactoring** — BaseAIAgent с Singleton ClientSession, исправлена утечка ресурсов в GigaChat
✅ **Pipeline Refactoring** — tasks.py декомпозирован на фазы (extraction, scoring, AI, finalize)
✅ **Repository Cleanup** — удалены env/, test scripts, IDE files, hypothesis cache; очищен .gitignore
✅ **Documentation Updates** — BUSINESS_MODEL.md, ROADMAP.md, ARCHITECTURE.md, README.md обновлены
✅ **Kiro Infrastructure** — добавлены .kiro/hooks/, .kiro/specs/, .kiro/steering/ для автоматизации и правил
✅ **Статистика** — 335 файлов изменено, 28177 строк добавлено, 13712 удалено

## Исправления сессии 2026-03-27 (LLM Financial Extraction — таски 4–11 + hotfixes)
✅ **`src/models/settings.py`** — добавлены 4 поля: `llm_extraction_enabled`, `llm_chunk_size`, `llm_max_chunks`, `llm_token_budget` с валидаторами.
✅ **`src/tasks.py`** — интегрирован `_try_llm_extraction`, inline-импорты вынесены на уровень модуля.
✅ **`src/analysis/nlp_analysis.py`** — заменён inline-промпт на `LLM_ANALYSIS_PROMPT`.
✅ **`tests/test_llm_extractor.py`** — 22 unit-теста.
✅ **`tests/data/llm_responses/`** — 5 fixture-файлов.
✅ **`.env.example`** — добавлена секция LLM Extraction.
✅ **`src/analysis/pdf_extractor.py`** — `_is_glyph_encoded()` (детектор кастомных шрифтов → OCR), `_get_poppler_path()` (Windows autodetect), порог `_normalize_number` снижен до 16 цифр, `_is_valid_financial_value` до 1e13.
✅ **`requirements.txt`** — добавлен `ghostscript~=0.8.1`.
✅ **`frontend/src/context/AnalysisContext.tsx`** — `MAX_POLLING_ATTEMPTS` = 600 (20 минут для OCR).
✅ **Agent Hooks** — созданы 4 хука: Python Lint on Save, Run Tests After Task, Architecture Guard, AGENTS.md Rules Reminder.
✅ **Poppler + ghostscript** — установлены локально.

## Исправления сессии 2026-03-27 (LLM Financial Extraction — таски 4-11 + hotfixes)
- src/models/settings.py: 4 новых поля LLM extraction с валидаторами
- src/tasks.py: _try_llm_extraction интегрирован, inline-импорты вынесены
- src/analysis/nlp_analysis.py: inline-промпт заменён на LLM_ANALYSIS_PROMPT
- tests/test_llm_extractor.py: 22 unit-теста
- tests/data/llm_responses/: 5 fixture-файлов
- .env.example: секция LLM Extraction
- src/analysis/pdf_extractor.py: _is_glyph_encoded, _get_poppler_path, порог 16 цифр и 1e13
- requirements.txt: ghostscript~=0.8.1
- frontend/src/context/AnalysisContext.tsx: MAX_POLLING_ATTEMPTS=600
- Agent Hooks: 4 хука созданы
- Poppler + ghostscript установлены локально

## Исправления сессии 2026-03-27 (LLM Financial Extraction — таски 1–3)
✅ **`src/core/prompts.py`** — создан: `LLM_EXTRACTION_PROMPT` (защита от prompt injection, 15 метрик с RU/EN синонимами, правила confidence, OCR-артефакты) и `LLM_ANALYSIS_PROMPT` (российские нормативные пороги, формат JSON-ответа).
✅ **`src/analysis/llm_extractor.py`** — реализован полностью: `_normalize_number_str`, `_apply_anomaly_check`, `parse_llm_extraction_response`, `chunk_text`, `merge_extraction_results`, `extract_with_llm`. Все функции покрыты property-тестами.
✅ **`tests/test_llm_extractor_properties.py`** — 19 property-тестов (Hypothesis): Properties 1–6, 8–11. Все зелёные.
✅ **Checkpoint таска 3** — пройден: 19/19 passed, регрессий нет.

## Исправления сессии 2026-03-27 (OCR + Qwen regression)
✅ **OCR Giant Number Bug** (pdf_extractor.py) — исправлен: паттерн `\d[\d\s,\.]*\d` заменён на строгий `\d{1,3}(?:[ \t\xa0]\d{3})+` (без `\n`), что предотвращает склейку чисел с разных строк OCR-текста в монстров типа `1234567890123344444`.
✅ **_normalize_number guard** (pdf_extractor.py) — добавлена защита: строки с >18 цифрами отклоняются как артефакты парсинга.
✅ **_NUMBER_PATTERN** (pdf_extractor.py) — обновлён: использует `[ \t\xa0]` вместо `\s`, не пересекает переносы строк.
✅ **_is_valid_financial_value порог** (pdf_extractor.py) — поднят с `1e14` до `1e15` (корректный верхний предел).
✅ **_extract_metrics_with_regex алиас** (tasks.py) — добавлен алиас для совместимости с тестами (БАГ 10).
✅ **MAX_POLLING_ATTEMPTS** (AnalysisContext.tsx) — исправлен с 60 на 15 согласно спецификации.
✅ **f-строки в логах** (pdf_extractor.py) — заменены на `%`-форматирование.
✅ **Exploratory тесты** (test_qwen_regression_exploratory.py) — БАГ 1 и БАГ 8 помечены `xfail` (баги исправлены).

✅ **WebSocket** (ws_manager.py + useAnalysisSocket.ts) — внедрена система real-time обновлений.
✅ **Refactoring** (tasks.py) — проведена декомпозиция "God Method" на фазы.
✅ **AI Core** (base_agent.py) — внедрён базовый класс и Singleton-сессии для всех провайдеров.
✅ **Linter & Imports** (app.py, tasks.py, agents) — исправлено более 30 ошибок импортов и предупреждений линтера.
✅ **Config** (settings.py) — централизованная загрузка `.env` через Pydantic.
✅ **БАГ 15** (gigachat_agent.py) — исправлен: внедрён Singleton `ClientSession`, убрана утечка ресурсов.
✅ **БАГ 16** (scoring.py) — улучшен: добавлено поле `confidence_score` для визуализации полноты финансовых данных.
✅ **БАГ 17** (pdf_extractor.py) — исправлен: улучшена детекция сканированных PDF через проверку наличия `/Image`.
✅ **БАГ 18** (analyze.py + pdf_extractor.py) — исправлен: regex-экстракция перенесена в слой анализа.
✅ **ТЕХ. ДОЛГ** (types.ts) — исправлен: удалён дублирующий файл `types.ts` на фронтенде.

---

## Что будет дальше
❌ Cleanup job operationalization: cron/Task Scheduler runbook, retention review, safe first-run checklist
❌ Test hygiene: `pytest-asyncio` config, unclosed `aiohttp` session path, warning cleanup, triage legacy `tests/test_tasks_coverage.py`
❌ Optional heavy/OCR real-PDF tier: отдельный медленный regression-layer вне fast CI path
❌ Heavy real-PDF performance pass: Cloudflare full-table path всё ещё тяжёлый и не укладывается в комфортный debug/runtime budget
❌ OCR/scanned curated corpus: отдельный regression-набор для действительно scanned/OCR-heavy PDF
❌ Production hardening: VPS smoke, HTTPS, backup/restore flow
❌ Дальше по roadmap: interactive OCR corrections, S3/MinIO, отраслевые benchmarks/OKVED

---

## Известные ограничения
- В режиме `TASK_RUNTIME=background` задача всё ещё живёт в памяти процесса и теряется при рестарте; персистентный контур требует `TASK_RUNTIME=celery` + Redis + worker
- Отмена задач в `TASK_RUNTIME=celery` пока best-effort: есть `revoke`, но нет полного распределённого контракта отмены/возобновления
- SlowAPI rate limiter — in-memory, не работает при нескольких инстансах
- Polling 2000ms × N пользователей = N/2 req/s к БД (захардкожено в `usePdfAnalysis.ts`)
- camelot-py + Tesseract + Poppler — ~500MB в Docker-образе, нельзя убрать без замены PDF-стека
- GigaChat требует кастомный SSL CA bundle и российский аккаунт Sber
- MAX_PDF_PAGES=100, MAX_FILE_SIZE=50MB, AI_TIMEOUT=120s — менять только везде одновременно
- **Production**: миграции запускаются отдельным сервисом `backend-migrate` перед стартом `backend`

---

## Архитектура в двух словах

```
React/Mantine (polling) → FastAPI routers → tasks.py (оркестратор)
  → pdf_extractor → ratios → scoring → recommendations → nlp_analysis
  → ai_service (GigaChat | DeepSeek | Ollama | None)
  → crud.py → PostgreSQL
```

Детали: `.agent/architecture.md` — структура слоёв, data flow, паттерны, критичные файлы, технический долг.
Ключевой файл оркестрации: `src/tasks.py` — содержит `RATIO_KEY_MAP`, `_build_score_payload()`, порядок вызовов pipeline.

---

## Production Architecture

```
Internet → nginx:80/443 (reverse proxy, rate limiting, gzip)
              ↓
        backend:8000 (internal network only)
         ↓        ↓
    db:5432   ollama:11434 (optional, profile-based)
```

**Сервисы:**
- `nginx` — единственный публичный сервис, reverse proxy, статика frontend
- `backend` — FastAPI, не exposed наружу, health check на `/system/health`
- `db` — PostgreSQL 16, internal only, health check через `pg_isready`
- `backend-migrate` — Alembic миграции, run once перед стартом backend
- `ollama` — local LLM, optional, через `--profile ollama`

**Файлы:**
- `Dockerfile.backend` — multi-stage (build → runtime), non-root user
- `frontend/Dockerfile.frontend` — multi-stage (node → nginx)
- `docker-compose.prod.yml` — production orchestration
- `nginx.conf` — reverse proxy, rate limiting (10r/s), security headers
- `scripts/deploy-prod.sh` — deploy script (validate → build → migrate → start)
