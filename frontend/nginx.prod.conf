# NeoFin AI Frontend - Production Nginx Configuration
# Serves React SPA + proxies /api/ to backend

# HTTP server block
server {
    listen 80;
    server_name localhost;

    # Document root
    root /usr/share/nginx/html;
    index index.html;

    # Logging
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log warn;

    # Gzip compression (reduces bandwidth, improves load time)
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript image/svg+xml;

    # Security headers (protect against common attacks)
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # ==========================================================================
    # SPA Routing (React Router)
    # If file/directory doesn't exist, serve index.html (client-side routing)
    # ==========================================================================
    location / {
        try_files $uri $uri/ /index.html;
    }

    # ==========================================================================
    # API Proxy (reverse proxy to backend)
    # All /api/ requests forwarded to http://backend:8000/
    # ==========================================================================
    location /api/ {
        proxy_pass http://backend:8000/;

        # Required headers for backend to see real client info
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support (if needed in future)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Timeouts
        proxy_connect_timeout 75s;
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;

        # Buffer settings
        proxy_buffering off;
    }

    # ==========================================================================
    # Static Assets Caching (immutable assets from Vite build)
    # Vite adds content hash to filenames → safe to cache for 1 year
    # ==========================================================================
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|wasm)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # ==========================================================================
    # HTML Files (never cache - need fresh version for SPA updates)
    # ==========================================================================
    location ~* \.html$ {
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }
}

# ==========================================================================
# SSL Server Block (enabled via envsubst if SSL_CERT_PATH is set)
# Usage: docker-compose with env_file containing SSL_CERT_PATH, SSL_KEY_PATH
# ==========================================================================
# server {
#     listen 443 ssl http2;
#     server_name localhost;
#
#     # SSL certificates (paths substituted via envsubst)
#     ssl_certificate ${SSL_CERT_PATH};
#     ssl_certificate_key ${SSL_KEY_PATH};
#
#     # SSL configuration (modern security standards)
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers off;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 1d;
#     ssl_session_tickets off;
#
#     # HSTS (force HTTPS for 1 year)
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     # Same configuration as HTTP block above
#     root /usr/share/nginx/html;
#     index index.html;
#     access_log /var/log/nginx/access.log;
#     error_log /var/log/nginx/error.log warn;
#
#     # ... (gzip, security headers, locations - same as above)
# }
